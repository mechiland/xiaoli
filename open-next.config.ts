// Bootstrapped by core (wave 1); owned by deploy from wave 4.
// All app pages are dynamic (per-user data), so no incremental cache override is configured.
import { defineCloudflareConfig } from '@opennextjs/cloudflare'

export default defineCloudflareConfig({})

# DECISIONS

Assumptions, trade-offs, and deviations from PLAN.md / SPEC.md. Append under `## <module>`; re-read this file right before editing (other agents append concurrently). Never include real names, phone numbers, addresses, or API keys.

Terminology: "PLAN" = PLAN.md (process), "SPEC" = SPEC.md (product; PLAN's "PLAN.md 第 N 节" references mean SPEC §N).

## architecture

### A1. Pinned versions (checked 2026-09-15)
| package | version | reason |
|---|---|---|
| next | 16.3.5 | latest; inside @opennextjs/cloudflare 1.20.6 peer range `>=15.5.24 <16 \|\| >=16.3.3` |
| react / react-dom | 19.3.0 | latest; next 16 peer `^19` |
| @opennextjs/cloudflare | 1.20.6 | latest; supports Next 16 (docs: all 16.x minors) |
| wrangler | 4.131.2 | satisfies OpenNext peer `^4.125.0`; remote bindings use `remote: true` (≥ 4.36) |
| better-auth / @better-auth/drizzle-adapter | 1.7.5 | latest; adapters split into separate packages since 1.5; peer drizzle-orm `^0.45.2` |
| hono | 4.13.8 | latest |
| @hono/zod-validator | 0.9.1 | latest; zod v4 |
| drizzle-orm / drizzle-kit | 0.45.2 / 0.31.10 | latest stable; matches better-auth peers |
| zod | 4.6.5 | better-auth depends on `^4.5.4` — same major avoids duplicate zod |
| @tanstack/react-query | 5.102.8 | latest |
| tailwindcss | 4.3.3 | latest; CSS-first config (`@theme` tokens in `app/globals.css`) |
| shadcn (CLI) | 4.21.0 | components copied into `components/ui`, themed to Loam |
| fflate | 0.8.3 | browser ZIP |
| lunar-typescript | 1.8.6 | lunar ↔ solar incl. leap months |
| pinyin-pro | 3.29.4 | pinyin initials for index |
| vitest | 5.0.0 | better-auth peer allows `^5` |
| @playwright/test | 1.63.0 | system Chrome via `channel: 'chrome'`; no browser download |
| typescript | **5.9.3** (not 7.0.2) | TS 7 is the native (Go) compiler with a new API; Next 16's type-check and several tools still target the JS compiler API — avoid being first adopter. 6.0 is a transition release. Core may bump after verifying `next build` typecheck. |
| tsx | 4.23.13 | run TS CLI scripts (seed, eval, verify) |
| node | v24.21.0 local | OpenNext runs in workerd with `nodejs_compat`; next engines `>=20.9` |

### A2. DeepSeek instead of Anthropic
SPEC §4 and §12 say Anthropic; PLAN overrides with DeepSeek `deepseek-flash`. Facts from api-docs.deepseek.com (2026-09-15):
- OpenAI-compatible base URL `https://api.deepseek.com` (`/chat/completions`); Anthropic-compatible at `/anthropic` (not used).
- Models: `deepseek-flash` (DeepSeek-V4.1-Flash), `deepseek-v4-pro`. 1M context, 384K max output.
- Flash price / 1M tokens: input cache hit $0.003–0.006, cache miss $0.15–0.30, output $0.60–1.20 (peak = 01:00–04:00 and 06:00–10:00 UTC Mon–Fri is 2× off-peak).
- JSON output: `response_format: {"type":"json_object"}`; prompt must contain the word "json" and an example; API may return empty content (treat as retryable); set `max_tokens` so JSON isn't truncated.
- **Thinking mode is the default for deepseek-flash.** OpenAI format toggle: `thinking: {type: "enabled"|"disabled"}` (in `extra_body` for SDKs; plain field for raw fetch). In thinking mode `temperature`, `presence_penalty`, `frequency_penalty` are accepted but ignored; `top_p` floored at 0.95; reasoning returned in `reasoning_content`.
- Temperature guidance table: coding/math 0.0, data cleaning/analysis 1.0, conversation/translation 1.3, creative 1.5; default 1.0.
- **Decision**: PLAN requires "temperature 取文档建议的最低可用值" → `temperature: 0` with `thinking: {type: "disabled"}` (so temperature takes effect and output tokens aren't spent on reasoning). The llm builder must confirm with ≤ 5 small live calls that non-thinking mode + json_object works for deepseek-flash and record the result here; if thinking mode yields materially better eval scores, extract may enable it via `LLM_THINKING=enabled` with a prompt version bump and recorded comparison.
- Direct `fetch` implementation (no OpenAI SDK) to keep the Worker bundle small.
- Secrets: `DEEPSEEK_API_KEY` in `.env.local` (Next dev) and must be mirrored into `.dev.vars` for wrangler/getPlatformProxy contexts; both gitignored. Deploy: `wrangler secret put`.

### A3. OpenNext + Next 16 caveats
- `next.config.ts` calls `initOpenNextCloudflareForDev()`; bindings via `getCloudflareContext()` (sync in route handlers/RSC; `{async:true}` for static generation — all our pages are dynamic, so sync is fine).
- **No `proxy.ts` / `middleware.ts`.** Next 16 renamed middleware to `proxy.ts` and it runs on the Node runtime; OpenNext Cloudflare lists "Node Middleware" as not supported. Auth gating is therefore done in `app/(app)/layout.tsx` (server-side `getAuth(env).api.getSession({ headers: await headers() })` → `redirect('/sign-in')`) and in Hono session middleware for `/api/*`. SPEC M0's "未登录访问 /people 跳转登录页" is tested as "unauthenticated `/` and `/p/:id` redirect to `/sign-in`".
- No `export const runtime = 'edge'` anywhere.
- DB clients per request (OpenNext docs): `getDb()` wrapped in React `cache()`; no module-level db/auth singletons.
- `.dev.vars` needs `NEXTJS_ENV=development`.
- Worker size limit 3 MiB (free) / 10 MiB (paid) gzip — deploy checks. Risk: lunar-typescript + pinyin-pro dictionaries in the server bundle. Mitigation: pinyin sort key computed on write (`persons.label_sort`) so pinyin-pro can be client/seed-only if needed; deploy critic measures.
- Next 16 dev uses Turbopack by default; OpenNext docs list Turbopack as supported. If `next build` output misbehaves with OpenNext, core/deploy may use `next build --webpack`; record here.

### A4. Better Auth on D1
- `getAuth(env)` per request (SPEC §4). Uses `drizzleAdapter(drizzle(env.DB), { provider: 'sqlite', schema })` from `@better-auth/drizzle-adapter` rather than Better Auth 1.5's native D1 auto-detection, so auth tables live in the same Drizzle schema and one `drizzle-kit generate` → `wrangler d1 migrations apply` pipeline (SPEC §4 says Drizzle adapter + sqlite provider).
- D1 has no interactive transactions; Better Auth uses `batch()` for D1. Our own multi-statement writes use `db.batch()` with idempotent status guards (ARCHITECTURE §4.2).
- Schema generation needs a static config: `server/auth.config.ts` (no env access) consumed by `npx auth@1.7.5 generate --adapter drizzle --dialect sqlite`.
- Known local pitfall: multiple Drizzle wrappers around the same local D1 binding in one request can hit SQLite WAL write locks — create one db per request and pass it to `getAuth`.
- `nextCookies()` plugin not needed (sign-in/up via client `authClient` → `/api/auth/*`), unless core uses server actions for auth.

### A5. SPEC/PLAN conflicts and resolutions
1. **SPEC M0 "侧边导航" and `/people`** vs SPEC §9.2 "不做侧边栏" and routes `/p/:id`. → §9.2 wins (it's the detailed IA). No sidebar; top bar only. No `/people` route. M0 acceptance test uses `/` and `/p/:id`.
2. **Anthropic → DeepSeek** (A2). `server/llm/` has a DeepSeek implementation only; adapter interface is provider-agnostic.
3. **Directory structure SPEC §5 vs module ownership.** SPEC lists `server/routes/{imports,people,chats,claims,review,home,search}`; to give disjoint ownership we split into per-module router files (ARCHITECTURE §1 table): e.g. `people.ts` (person) vs `people-actions.ts` (review: merge/split/manual add), `chats-list.ts` (import) vs `chats.ts` (chat), `imports-review.ts` (review). There is no `claims` router — claim actions go through `/api/review/claim/:id` and `/api/people/:id/claims`.
4. **`prompts/` ownership**: SPEC §5 puts prompts at repo root `prompts/extract.v1.md`; owned by extract. Eval judge prompt lives under `eval/prompts/` (eval-synthetic) to keep prompts/ single-owner.
5. **Gold path split and gold authorship**: PLAN says `eval/gold/<文件名>.json`. → `eval/gold/synthetic/<zip>.json` (committed) and `eval/gold/real/<zip>.json` (gitignored). Reason: a single dir can't be partly gitignored by file name reliably, and real gold contains real names. **Who writes gold**: both synthetic and real gold are written by the independent **annotator** role (ARCHITECTURE §1.16, §7.6), NOT by eval-synthetic (which runs the eval and sees pipeline output). eval-synthetic only generates ZIPs plus `fixtures/synthetic/<zip>.intent.json` (planted traps), which the annotator may read only after freezing. This matches PLAN §2 (blind, item-by-item annotation); what we add beyond PLAN is the freeze mechanism (revised round 3, A7 #3): one committed, append-only `eval/gold/LOCK.json` with a version history per zip (real zips keyed by `real-<16 hex of ZIP sha256>`, hashes only), `goldSha256` in every EvalReport. Gates fail when a gold file differs from the last LOCK version, when any version after the first lacks a matching `gold-change: <lockKey> <sha12>` line under `## annotator` in this file, or when the working LOCK is not an append of the committed one.
6. **Report path split**: PLAN `eval/reports/<时间戳>.json` is kept for the combined **aggregate-only** report (real zip names anonymized as real-1..n); per-item detail for real data under `eval/reports/real/` (gitignored); synthetic detail under `eval/reports/synthetic/`.
7. **PLAN M5 error taxonomy** has 5 classes; PLAN §2 lists 6 (adds 证据引用了窗口外或不存在的消息). → Use PLAN §2's 6 + `other`.
8. **ReviewAction enum**: SPEC §7 `accept|reject|edit|merge|split|supersede`; SPEC §9.5 has a hard "删除" for confirmed claims (not into history). → add `delete`.
9. **"改写" history**: SPEC §9.5 says edited original moves to 历史; §9.9 says edit in import-result confirms with edited content. → person page edit of a confirmed claim creates new confirmed claim + old row `superseded` with `status_reason='edited'`; import-result edit of a proposed claim updates in place and confirms (reviewLog keeps `before`), no history row (it was never confirmed).
10. **"已过时"** without replacement: SPEC status enum has no `outdated`. → status `superseded` + `status_reason='outdated'`, `valid_to` set to today (YYYY-MM-DD).
11. **handles unique `(ownerId, kind, value, chatId)`** with nullable chatId: SQLite treats NULLs as distinct → unique expression index on `ifnull(chat_id, 0)`.
12. **messages unique `(chatId, seq)`** + LCS reordering: seq assigned with gaps of 1024; renumbering is a two-phase batch. Prompts use per-window local seq numbers, mapped back to message ids — so "evidence seq in window" validation uses local indices.
13. **Importing duplicates**: SPEC 9.7 checks duplicate in step 1 (before upload). → added `POST /api/imports/check {sha256}`; `POST /api/imports` still enforces uniqueness (409). An import left in status `mapping` does not count as a duplicate (A7 #2).
14. **`import_messages` table** added (not in SPEC §7) to show per-import message ranges on the chat page and support delete semantics; **`claim_mentions`**, **`user_settings`**, **`llm_calls`** added. `imports.chatId` nullable (import exists before mapping).
15. **messages.sender_name** added: SPEC delete-person says "sender 置为未知" but chat page should still show the raw display name for unlinked senders (§9.10). We keep the raw name; the person link is removed.
16. **Retries**: PLAN "重试最多 2 次" = up to 3 attempts per window total, counted across `jobs/next` calls (job-level), not inside one request (30 s budget).
17. **Budgets on dev server** (revised round 2): one scenario `_smoke/perf-budgets`, one statistic: 4 sequential loads, first discarded, **median of loads 2–4** ≤ 300 ms (document TTFB and `Server-Timing` of the page's API). The gate runs against `next dev` on :3000. Production-like confirmation uses `pnpm preview:prod` = `opennextjs-cloudflare build && opennextjs-cloudflare preview` (OpenNext CLI docs, checked 2026-09-15: preview populates the local cache and serves the built worker through `wrangler dev`, i.e. workerd with the local `.wrangler/state` bindings; wrangler dev default port 8787). `next start` was dropped: `initOpenNextCloudflareForDev` targets `next dev`, so `next start` would not get D1/R2 bindings. Preview is integrator-only (second process, memory-tight machine). If dev fails only from dev overhead while preview passes, record both logs here; the critic may accept.
18. **Eval gating with tiny real samples**: the three real ZIPs contain ~136 messages total (counts only). Precision/recall on real data will have very few items (likely < 30 gold claims), so a single error moves precision by several points. Gates are evaluated on real and synthetic separately and both must pass; synthetic ZIPs must be substantially larger and cover the transactional/sensitive/inference traps. Gold (real and synthetic) is frozen after annotation (A5 #5); no edits to raise scores, per PLAN.
19. **STATUS.json modules**: PLAN lists 13 modules; we add `verify-seed` and `eval-synthetic` infra modules. The annotator is a role without code or critic, so it has no STATUS entry.
20. **Ownership moves made for the wave rule** (round 2): a module may only depend on modules of the same or an earlier wave. So: `PersonPicker` → search (wave 2; PLAN lists it nowhere, SPEC §9.7 needs it in the wave-2 import overlay); Evidence component → review (wave 2; PLAN puts "证据组件" under person, wave 3, but review owns the evidence API and both wave-3 pages consume the component); `POST /api/people` → review; `GET /api/people?index=pinyin` + `listPeopleIndex` → search; `GET/PATCH /api/settings` → core (read by the wave-2 import overlay and wave-3 home/welcome/import-result). Core ships placeholder pages and components so wave-2 flows never hit a 404. Deviation from PLAN §1's list, which assigns 证据组件 to person.
21. **`/welcome` route**: not in SPEC §9.2's route list. SPEC §9.12 requires a single-page onboarding right after sign-up, so we add `/welcome` (home module). Core's sign-up page redirects there after success; `(app)/layout` does not force it (SPEC says it can be skipped). Both save and skip set `onboardedAt`.
22. **invalid-evidence gate reading**: PLAN §4 "证据引用无效消息：0". Extract's validation drops items whose evidence is outside the window, so gating the pipeline's own post-filter count would always pass. We gate `invalidEvidencePost`, **recomputed independently by the harness** from the parsed export and each item's window range (so a bug in the filter, id mapping or persistence still fails the gate). This follows the product reading: no stored item points at an invalid message. The raw model rate before filtering (`invalidEvidencePreRate`) is reported, with a warning above 5%, and not gated: dropped items already lower recall, which is gated.
23. **Strict vs lenient claim matching**: judge verdicts `same | less_specific | different` (an added unsupported detail = `different`). The precision gate (≥ 0.85) uses lenient (same + less_specific), because a vaguer but true statement is not a wrong record. The recall gate (≥ 0.70) uses strict (same only), because a vaguer statement doesn't recover the fact. Both variants are reported.
24. **Delete import vs messages shared with later imports**: SPEC §7 says to delete the messages the import "首次引入". Read literally, deleting import A would also remove messages that a later import B re-contained (and B's evidence with them). We instead reassign `first_import_id` to the earliest other import that contained the message (via `import_messages`) and delete only messages that no other import contained. We believe that is the intent: B still has those messages.
25. **Error taxonomy mechanics**: false positives are labelled in two steps. Deterministic labels first: invalid_evidence, sensitive_leak, and wrong_person for handles/relations/dates. Then an LLM judge sees the FP text, its evidence window (evidence ±2 messages), all persons' gold claims and all negatives, and outputs one label plus a `subLabel` for should_ignore. `transactionalAsClaimRatio` counts claims labelled should_ignore/transactional|coordination, so transactional claims the annotator did not list as negatives are counted too.
26. **Gold drift guard**: gold stores `parserVersion`, `messageCount`, `messagesSha256` (sender/time/body per idx, kind excluded so reclassifying a kind doesn't invalidate gold) and anchors. On mismatch eval refuses to score that zip.
27. **Offline prompt inputs**: `extractOffline` passes only what a real user would have typed at mapping time: sender persons labelled with their senderName, and the gold chat title, which the annotator must derive from sender names. Gold person labels and aliases, claims and negatives never reach the prompt.
28. **Token budget scope**: one counter per loop covers in-app extraction (dev server), eval and judge calls: `.dev/llm-budget.json` in dev/CLI. When deployed, the budget is the sum over `llm_calls` since `LLM_BUDGET_SINCE`, default the current UTC month. The orchestrator runs `pnpm llm:usage --reset --loop <id>` at loop start and copies `pnpm llm:usage --json` into STATUS.json `llmUsage`.
29. **Model choice** (revised round 3): `settings.extractModel` ∈ {`deepseek-flash`, `deepseek-v4-pro`} or null; `SettingsDTO.extractModel` is nullable and `getUserSettings` never fills a default, so null falls through to env `EXTRACT_MODEL` → `deepseek-flash`. `EXTRACT_MODEL` is validated as the same enum by `server/env.ts` (an unknown value is a config error, not silently ignored). `processNextJob` receives `env` in its opts and resolves per request; eval uses `--model` or the same env resolution. Settings UI shows "默认（deepseek-flash）" for null.
30. **Per-request deadline**: `jobs/next` has a 28 s deadline. The extract call gets ≤ 20 s. Dedup runs only if ≥ 4 s remain, else it is skipped (treated as no duplicates, counted in the eval report), so one window stays under SPEC/PLAN's 30 s budget. Eval's `extractOffline` applies the same per-attempt deadline by default (A7 #4).
31. **Health endpoint auth**: core's session middleware rejects unauthenticated requests centrally for everything except `/api/auth/*` and `GET /api/health`, so routes can't forget auth. SPEC §10 says "所有路由经 Better Auth session 中间件": health is the one deliberate exception, needed by the verify tool before login.
32. **Delete-all/export table lists** are explicit (ARCHITECTURE §11) and include `llm_calls` (raw_output is derived from the user's messages) and `extraction_jobs`. Better Auth tables are kept by delete-all.
33. **Hand-written failure cassettes** live in the testing module (`server/extract/__fixtures__/cassettes/`). `fixtures/cassettes/{synthetic,real}` hold only record-run output.
34. **Showcase pages for shared components** live at `/dev/<module>/<name>` (`app/(app)/dev/<module>/**`, module-owned; core's `dev/layout.tsx` returns 404 outside development).

### A6. Things SPEC leaves open (defaults for builders)
- **Group chat display names**: `display_group` handles are chat-scoped (`chatId` set); `display_private` also chat-scoped (the remark name applies to the account, but we can't know that). `mentioned` / `address_term` from `@显示名 称呼` are chat-scoped; `real_name` is global (`chatId` null). Recommendation for mapping uses exact normalized match of display name across any chat, reason text names the chat ("在『<chat>』中也叫这个名字").
- **Self display name drift**: user's own WeChat name may change between exports → `selfDisplayNames` is a list; mapping auto-selects self on any match.
- **Unseen message formats** (quote reply, recall, merged-forward, contact card, location, file, link, group system messages, Android): parser classifies recognizable prefixes when obvious (`[引用]`/"「…」\n- - -", "撤回了一条消息", `[文件]`, `[链接]`, `[位置]`, `[名片]`, `[聊天记录]`) into kinds `quote|recall|file|link|location|contact_card|forward|system`, otherwise `unknown` preserving raw text. Lines not matching the header rule stay in the previous body (never dropped). Android format: detection returns ParseError `no_messages` if no header matched, with UI message per §9.7. Synthetic ZIPs include invented-but-plausible variants, clearly labeled as guesses in the generator.
- **Pinyin grouping for non-Chinese labels**: first char Latin letter → that letter (uppercase); Chinese → pinyin-pro first letter (polyphonic surnames use surname mode: 曾→Z, 单→S, 解→X, 仇→Q, 区→O); digits, emoji, symbols, other scripts → `#` group, listed last. Sort within group by `label_sort` (full pinyin, then label).
- **Lunar leap months**: `important_dates.is_leap_month`. A leap-month lunar birthday recurs on the leap month only when that year has the same leap month; otherwise falls back to the regular month of the same number (common Chinese practice). Lunar day 30 in a 29-day month → day 29. Upcoming dates display "农历八月十五 · 9月25日"; solar Feb 29 in non-leap years → Feb 28.
- **"最近有新信息的人"**: persons with claims confirmed in the last 5 imports' review actions (by `review_log.created_at` of accept on items with those import ids), latest first, max 10.
- **"与我的关系"**: confirmed relation where one side is the self person; value shows `label` if present else Chinese of `type`.
- **High-confidence bulk confirm** (revised round 2): only **claims** carry confidence (SPEC §8.6). "确认所有可信度高的条目" = proposed claims of this import with `confidence >= settings.highConfidenceThreshold` and `sensitive = false`. Handles, relations, dates and events have no confidence and are never included; the user confirms them per item or with "本节全部确认". `ImportReviewResponse.highConfidence` lists the exact ids so the count shown equals what gets confirmed.
- **Timezone**: messages are naive local time; "today" and ISO display use `APP_TZ=Asia/Shanghai`.
- **exportedAt from ZIP filename**: interpreted as Asia/Shanghai local time → ISO with offset converted to Z. Missing/renamed file → null.
- **Chat kind with >2 senders** preselects group; exactly two preselects private but requires confirmation (SPEC).
- **Attachment upload limits**: images ≤ 20 MB each; videos unchecked by default and, if checked, ≤ 100 MB (R2 put from Worker).
- **Attachment upload lifecycle** (revised round 2; ARCHITECTURE §1.4): single owner = **import**. The overlay keeps the ZIP `File` in a module-level store keyed by importId; after mapping a module-level runner uploads selected media sequentially, independent of which page is mounted, so it continues on the import-result page (which only mounts import's `<AttachmentUploadStatus>`). After a reload the bytes are gone: server rows stay `selected=1, r2_key=null`, and the status line offers "重新选择这份文件继续" (sha256 must match). If ignored, the chat page shows "图片未导入". Holding the `File` (not unzipped bytes) keeps memory low.
- **Search**: SQL LIKE on `value_norm` / `statement_norm` (NFKC, lowercase, full-width → half-width), plus pinyin initials match on `label_sort` for Latin queries. No FTS5 in v1 (D1 supports FTS5 but not via drizzle-kit migrations cleanly; revisit if latency > 300 ms).
- **Evidence for manual items**: no evidence rows; UI shows "手动添加于 YYYY年M月D日" from `createdAt`.

### A7. Round-3 decisions
1. **Where the parsed export waits between step 1 and step 2: R2 staging.** Options were (a) R2 object, (b) chunked D1 staging table, (c) send messages again with the mapping request. Chosen: (a). `POST /api/imports` stores `StagedImportPayload` at `u/<ownerId>/imp/<importId>/parsed.json`; mapping reads it, runs LCS alignment against it, inserts messages, `import_messages`, then attachment rows (they need `message_id`), sets `extracting`, deletes the object. Why not (b): D1 caps a single value/row at about 2 MB (Cloudflare D1 limits page; not re-checked this round), so 50k messages need many chunk rows and a reassembly step, which is more code for no benefit. Why not (c): the ~10–15 MB body would be uploaded twice, and a failed or retried mapping would need the client to still hold the parsed export. R2 is already bound, and delete-all's `u/<ownerId>/` prefix delete covers the object. Owner: import.
2. **Abandoned imports (closed during step 2).** The overlay deletes the import when closed at step 2 (keepalive `DELETE`). As a safety net for a closed tab, `check` ignores `mapping` rows and `POST /api/imports` deletes a stale `mapping` row with the same sha before creating the new one, so `unique(owner_id, file_sha256)` never blocks re-importing. No resume flow: step 2 only asks for choices, which are quick to redo. `/imports/:id` for a `mapping` import shows "这次导入没有完成" + delete. Home's recent imports exclude `mapping`.
3. **Delete-import UI owner: import-result.** SPEC §9 places no delete-import control; PLAN §5's final flow needs one. It goes at the foot of `/imports/:id` as a quiet text button with a confirm dialog, then goes to `/`. Quiet and at the bottom so it does not compete with review actions (SPEC §9.3 tone). The chat page's import records only link to that page.
4. **Gold LOCK is append-only and committed for both sources.** The round-2 LOCK kept no history, so rewriting gold and LOCK together was undetectable, especially for gitignored real gold. Now `eval/gold/LOCK.json` holds `versions[]` per zip. Real zips are keyed by a hash of the ZIP (no names), so git history covers real gold too. freeze-gold only appends, and requires the DECISIONS `gold-change:` line before appending. Eval checks every later version has its line and that the working LOCK extends `HEAD`'s. `eval/gold/real/LOCK.json` no longer exists.
5. **Eval measures window time under the app's deadline.** `extractOffline` defaults to `deadlinePolicy: 'app'` (per attempt: deadline 28 s, extract ≤ 20 s with no transport retries, dedup only with ≥ 4 s left). `p95WindowMs` = nearest-rank p95 over all window attempts of extract + validation + dedup + persistence time; in replay it uses recorded call latency plus local compute. P4's gate is the integration test; the live/record eval p95 is its confirmation on real model latency. Consequence: eval now counts app-deadline timeouts as failed attempts, as the app does.
6. **Link tables carry `owner_id`.** `import_messages`, `claim_mentions`, `event_participants` and `evidence` have `owner_id` + `created_at`, a composite pk and no surrogate id. Every query can use `owned()`, the owner lint needs no exceptions, and delete-all/export work by owner alone. The cost is one text column per link row.
7. **Auth client** lives in core at `lib/auth-client.ts` (`createAuthClient` from `better-auth/react`), used by auth pages, the TopBar sign-out, and settings.
8. **`getAppLlm` is async** because the deployed budget reads D1 and the dev budget reads a file; handlers `await getAppLlm(c)`.
9. **Blind comparison (PLAN §5)**: the orchestrator runs it in wave 4 with synthetic seed-account screenshots vs Monica's public screenshots, randomized A/B, and a fresh judge agent. All files go to gitignored `artifacts/blind-judge/`, the result is summarized under `## integrator`, and it is not a gate (ARCHITECTURE §12.2). Third-party images are not committed.
10. **Annotator reads only `## architecture` and `## annotator` of this file.** Other sections (extract, llm, eval-synthetic, integrator) may describe pipeline output or prompt comparisons, which PLAN §2 keeps from the annotator.
11. **Destructive verify scenarios** declare `destructive: true`; the verify tool re-seeds that account afterwards. The seed dataset has a dedicated `delete-me` import so deleting it never disturbs other scenario tags.

## core

Round 1 (2026-09-15). Additive to ARCHITECTURE; nothing here changes a documented contract shape.

- **C1 pnpm 12 policies.** `pnpm-workspace.yaml` uses `allowBuilds` (esbuild, workerd, sharp) because pnpm 12 ignores `onlyBuiltDependencies`, and sets `minimumReleaseAge: 0` because several A1-pinned releases (hono 4.13.8, wrangler 4.131.2, better-auth 1.7.5 adapters, workers-types) are < 24 h old and the default age gate rejects the lockfile, which also blocks `pnpm run`. Versions are exact pins, so the gate adds nothing.
- **C2 Auth tables.** `server/db/schema/auth.ts` is generated by `npx auth@1.7.5 generate` and kept as generated: Better Auth's own tables store `integer` ms timestamps. The ISO-string convention (ARCHITECTURE §3) applies to business tables only.
- **C3 handles unique index is a custom migration.** drizzle-kit 0.31.10 emits invalid SQL for the `ifnull(chat_id, 0)` expression index, so `drizzle/0001_handles_unique_chat_scope.sql` was created with `drizzle-kit generate --custom` and holds the index. Schema file carries a comment. Future `db:generate` runs keep it.
- **C4 False Better Auth "schema mismatch" log.** The CLI-only `auth` instance in `server/auth.config.ts` (empty db, no schema) was instantiated whenever `getAuth` imported `authOptions`, and its schema check logged "missing tables user, session, account, verification" on every request. That instance now sets `advanced.database.validateSchema: false`. The runtime `getAuth` keeps Better Auth's default validation against the real schema (clean in dev and in vitest).
- **C5 Middleware order.** A `platformMiddleware` sets `c.var.env` (validated, cached per source object) and `c.var.db` before the Better Auth `/auth/*` handler, because that handler needs both; then Server-Timing, then session (central 401 except `/api/health`). `buildApp(deps)` exists for tests (`platformEnv`, `resolveUser`, `llmOverride`); `app = buildApp()` is the running app. `c.var.llmOverride` is a test-only hook the llm module's `getAppLlm(c)` may honour so `createTestApp({ llm })` works; always undefined in the app.
- **C6 RSC session helper.** `server/session.ts` exports `getServerUser()` (React `cache`) for `app/(app)/layout.tsx` and `app/(auth)/layout.tsx`; kept out of `server/auth.ts` so CLI scripts importing `getAuth` never load `next/headers`. `(auth)` pages redirect signed-in users to `/`.
- **C7 Env schema.** `ServerEnvSchema` has `DB`, `R2`, `BETTER_AUTH_SECRET`, `DEEPSEEK_API_KEY` optional so `parseServerEnv(process.env)` works in CLI/eval; the platform middleware requires `DB` and `getAuth` requires the secret, failing with the variable name only. Empty strings count as unset.
- **C8 `cloudflare-env.d.ts` is hand-maintained.** `wrangler types` also emits runtime types that clash with `@cloudflare/workers-types`.
- **C9 Cross-module scripts.** `seed`, `verify`, `verify:list`, `eval`, `eval:*`, `extract:offline`, `llm:usage` run through `scripts/run-owned.ts <entry> <owner> -- args`, which exits 1 naming the owning module when the entry file does not exist yet. `pnpm eval` = `eval/src/cli.ts run`; `eval:<cmd>` = `eval/src/cli.ts <cmd>` (eval-synthetic implements those subcommands). `verify:list` passes `--list`.
- **C10 Loam on shadcn.** Tokens in `styles/tokens.css`, mapped in `app/globals.css` (`@theme inline`). All shadow utilities resolve to none and a global rule removes `box-shadow`/`text-shadow` (Tailwind rings included). Radius utilities cap at 2px, so `rounded-full` in switch, radio, checkbox is also 2px (SPEC §9.1 "2px 以内"). Focus is a 1px outline, and inputs use a darker hairline instead of a ring. Overlay scrim is a light warm wash, not black. Loam colour names for modules: `ground, paper, paper-hover, highlight, line, line-strong, ink, ink-2, ink-3, proposed, superseded, danger`; helper classes `loam-link`, `loam-text-button`, `loam-proposed`, `loam-superseded`, `[data-highlight=true]`. shadcn CLI added a bogus `cn` npm dependency and `from "cn"` imports; both removed.
- **C11 Fonts.** `next/font/google` Noto Serif SC (500/600/700, `preload: false`, CJK slices self-hosted) and Inter as CSS variables; body uses the system CJK sans stack (PingFang SC / Noto Sans CJK SC …). Base body 15px / line-height 1.8, prose 16px / 1.9.
- **C12 Contract extras** (additive): `DayString`, `ClaimStatusReason`, `ApiErrorCode`, `AttachmentKind`, `CalendarSchema`, `NextOccurrenceSchema`, `RecentImportSchema`, `ReviewParamSchema`, `AttachmentNameParamSchema`, `ChatsListQuerySchema`, `ChatMessagesQuerySchema`, `EvidenceQuerySchema`, `SearchQuerySchema`, response schemas per route, `MAX_IMPORT_MESSAGES`, `MAX_IMAGE_BYTES`, `MAX_VIDEO_BYTES`, `DELETE_ALL_CONFIRM_TEXT`, `DEFAULT_HIGH_CONFIDENCE_THRESHOLD`, `RELATION_TYPES`, `DATE_KINDS`. Primitives are exported both as `X` and `XSchema`. `PATCH /api/settings { onboarded: true }` sets `onboardedAt` only when unset; `selfDisplayNames` are trimmed and de-duplicated.
- **C13 `AGENTS.md` / `CLAUDE.md`** at repo root are written by `next dev` 16.3 itself (agent rules). Core left them untouched; integrator decides whether to commit them or set `agentRules: false` in `next.config.ts`.
- **C14 Expected console error.** A wrong-password sign-in returns Better Auth's 401, which Chrome logs as "Failed to load resource". Verify scenarios that test a wrong password should list `POST /api/auth/sign-in/email 401` as an expected failure; the happy path produces zero console errors.
- **C15 Not created in round 1:** `eslint.config.mjs` / `.prettierrc` (Next 16 no longer lints during build; typecheck + owner lint test cover correctness) and `verify/scenarios/core/showcase.scenario.ts` (needs `verify/lib` from verify-seed; core adds it once `defineScenario` exists). `TooltipProvider` is not mounted (no tooltip is used yet).
- **C16 Bootstrap deploy files.** `open-next.config.ts` has no incremental cache (all pages are per-user dynamic). `wrangler.jsonc` uses a placeholder `database_id` for local D1 and bucket `xiaoli-files`; deploy replaces them in wave 4.

## annotator

- **W2-G1 (orchestrator, 2026-09-15)**: 第 1 波的 7 份金标准（4 synthetic + 3 real）全部作废，移到 `.dev/superseded-gold/superseded-wave1/`（gitignored），第 2 波由全新的标注 agent 按 ARCHITECTURE §7.6 从零重标。原因：(1) real 金标准是在 parser 和标注工具存在之前手写的，3 份里 2 份没通过 validate-gold，messagesSha256/idx 与 parser 对不上；(2) 检查第 1 波标注 agent 的 transcript，7 个里 6 个读了整份 DECISIONS.md（§7.6 只允许读 `## architecture` 与 `## annotator` 两节），其中可能包含 synthetic 生成器埋点的说明，synthetic 金标准的盲标不可信。没有 agent 读过 intent.json，也没有抽取管线输出（当时不存在）。这些金标准从未 freeze，不适用 gold-change 规则；新标注 agent 不得读取 superseded 文件。
- **W2-G2 (orchestrator)**: 第 1 波标注 agent 写在本节的推理笔记同样移到 `.dev/superseded-gold/superseded-wave1/decisions-annotator-wave1.md`（gitignored），避免新标注 agent 通过允许读取的本节看到旧金标准的判断。
- gold-change: 聊天记录_20260405_223012 39f5ec7f20c0 — 补漏：idx 172（3月12日"深圳有家做出行的在聊，蓝鲸出行，岗位是技术负责人"）只是在谈岗位，诱导推断"已在蓝鲸出行工作"，按 SPEC §8.7"不推断原文没有说的"应列为 inference_trap；同一段对话的 170（n15）、173–174（n13）已列，172 属漏标。新增 negative n18，其余不变。
- gold-change: 聊天记录_20260910_183020 588eaea919f8 — 补漏：idx 27（果果奶奶"我在校门口了，果果今天几点放学"）是 SPEC §8.7 点名的即时协调（"我在门口"），应列为 coordination negative；同类到达通知 117–118（n14）和紧接着的 28（n12）都已列入，27 属漏标。新增 negative n28（evidence [27]），c22/r4 仍引用 27 作为证据，其余不变。

## parser

Round 1 (2026-09-15). Module `lib/wechat-export`; public entry `@/lib/wechat-export` with the exact ARCHITECTURE §1.2 signatures (plus `isParseError`, `STICKER_CODES`, type re-exports).

- **P1 Splitting rule is the literal SPEC §6 rule.** A line whose first char is U+00B7 `·` AND whose next line matches `^\d{4}年\d{2}月\d{2}日 \d{2}:\d{2}$` starts a message; everything else (including `·` lines not followed by a timestamp, bare timestamp lines, consecutive `·` lines) stays in the previous body. Body = lines up to the next start with leading/trailing whitespace-only lines removed; inner blank lines and indentation are kept verbatim; no NFC/trim of the sender name (raw, so fingerprints stay stable). Other bullets (`・`, `•`) and other time formats (`2026-09-01 10:00:00`, `2026/09/01`) are NOT accepted: guessing Android formats would risk mis-splitting iOS bodies; such files get ParseError `no_messages` (A6). Unavoidable ambiguity: a body line `·x` directly followed by a line that is exactly a WeChat timestamp is split as a new message.
- **P2 Tolerances before splitting**: leading BOM stripped; CRLF and lone CR → LF; trailing spaces/tabs on the timestamp line ignored. Text before the first header is dropped with warning `preamble_text`. Empty sender (`·` alone) is kept as `''` with warning `empty_sender`. Empty body → kind `text`, body `''`.
- **P3 Warnings** (`{line, code}`, 1-based header line, 0 = file level): `preamble_text`, `empty_sender`, `attachment_no_name`, `attachment_missing`, `unknown_kind`, `time_out_of_order`, `duplicate_media_name`, `multiple_txt`, `decoded_gbk`. Never fatal.
- **P4 Kinds.** Tag = `[xxx]` at body start followed by space/end of line. Known tags from samples map per SPEC §6. Unseen-but-plausible tags are guesses: `[文件]`→file (meta.fileName, gets attachment linking), `[链接]`→link (title+url), `[位置]`→location, `[名片]`/`[个人名片]`→contact_card, `[聊天记录]`→forward, `[引用]`→quote, `[撤回]`→recall, `[系统消息]`→system, `[微信转账]`→transfer, `[表情]`→animated_sticker, `[语音通话]`→video_call with `meta.label` "语音通话…" (no voice_call kind in the contract; it's a call, not a voice message). Untagged heuristics (single line only): recall = `"名字" 撤回了一条消息` / `你撤回了一条消息`; system = quoted-name forms of 邀请…加入了群聊 / 修改群名为 / 移出了群聊 / 拍了拍 / 通过扫描二维码加入群聊 / 你已添加了…; quote = a `「名字：原文」` block on its own line(s) either trailing the reply or followed by a `- - -` separator (WeChat's copy format). Plain prose like 我拍了拍他 stays text. Body is always kept raw; meta is a hint.
- **P5 sticker_code vs unknown.** Whole body made only of `[xxx]` tokens: ≥ 2 tokens → sticker_code; a single token → sticker_code iff it is in the built-in WeChat emoji list (`STICKER_CODES`, ~250 CN/EN names), otherwise `unknown` with `meta.label`. `[emoji] text` or text containing codes → text. An unrecognised `[xxx] rest` prefix (xxx not an emoji code) → unknown; `[xxx]rest` without a space → text.
- **P6 Attachments.** `attachmentName` = the matching `media[].name` (exact, then NFC/case-insensitive); empty filename → null + `attachment_no_name`; filename present but not in the ZIP → null + `attachment_missing` (the name stays in `meta.fileName`). Import creates the attachment row with `file_name` null / `selected` 0 for both (ARCHITECTURE §1.4). `media` = every non-txt entry (skips directories, `__MACOSX/`, `._*`, `.DS_Store`); kind by extension (images jpg/jpeg/png/gif/webp/heic/heif/bmp, videos mp4/mov/m4v/3gp/webm, else file); `referenced` = some message links it. Media bytes are not inflated during parsing (fflate filter; sizes from the central directory), so parsing cost doesn't grow with video size.
- **P7 ZIP entry names.** WeChat iOS writes UTF-8 names without the ZIP UTF-8 flag (verified on all 3 real samples); fflate then decodes them as latin1. Names are re-decoded strict UTF-8 → strict GBK → left as is. txt choice: `聊天记录.txt` (shallowest) else the largest `.txt`. Text decoding: UTF-16 by BOM, strict UTF-8, strict GBK (warning), else ParseError `bad_encoding`.
- **P8 Hashes.** `fingerprint` = FNV-1a 64 over the UTF-8 bytes of the literal contract template `${senderName}${sentAt}${kind}${body}` (no separators, as written), computed with 16-bit limbs (no BigInt); tested against a BigInt reference. `sha256Hex` uses `crypto.subtle` and falls back to a built-in pure-JS SHA-256 when `subtle` is missing: browsers hide it on insecure origins, e.g. the dev server opened by LAN IP over http. `parseExportText` has no ZIP bytes, so `sha256` is `''`, and it throws `ParseError('no_messages')` like the ZIP path.
- **P9 exportedAt**: `YYYYMMDD_HHMMSS` anywhere in the file basename, Asia/Shanghai (fixed UTC+8) → ISO Z; invalid dates → null. **Senders** in `ParsedExport` are in first-appearance order; `summarize()` sorts by count desc (ties by first appearance). Preview images/videos = media entries in the ZIP (what can be uploaded), not message counts.
- **P10 Mentions** (`extractMentions`): the name ends at U+2005 (what WeChat inserts; seen in samples), with a plain space as fallback. `addressTerm` = the next token of ≤ 4 chars when a space, punctuation or end of body follows it. This is a candidate only (`@x 你好` yields 你好); extract/LLM decides. Emails are skipped.
- **P11 PARSER_VERSION** = `wechat-export@1`.
- **P12 Perf.** Node (vitest `perf.test.ts`, 5001 messages, 16 MB ZIP with 40 images + 8 MB video): median ≈ 30 ms, assert ≤ 1.5 s (P1b). System Chrome 153 (Playwright, `lib/wechat-export/scripts/browser-perf.ts`, same ZIP, https route so WebCrypto is used): first run 64 ms, median of 3 = 50 ms, budget 3 s (P1). vitest 5 no longer exports `bench`, so P1b is a timed test, not a `vitest bench` file.
- **P13 Real-sample check** (`npx tsx lib/wechat-export/scripts/check-real.ts`, counts only): messages 33 / 37 / 66, equal to an independent `awk` count of `·` lines followed by a timestamp line, and to `grep -c` of timestamp lines. All kinds seen in the samples classify with zero `unknown`; every non-empty image/video name links to a ZIP entry. The 2 empty image names in one sample give `attachment_no_name`.

## llm

Round 1 (2026-09-15). Builds on architecture A2. Contract shapes in ARCHITECTURE §5 are unchanged; the additions are listed in L13.

- **L1 DeepSeek docs re-checked 2026-09-15** (api-docs.deepseek.com: create-chat-completion, quick_start/parameter_settings, guides/json_mode, guides/thinking_mode, quick_start/rate_limit, quick_start/error_codes, guides/kv_cache). Facts not already in A2:
  - Thinking is **on by default** (effort `high`). There is also `thinking.reasoning_effort` (`none|low|high|max`).
  - `max_tokens` defaults to 8K without thinking and 64K with it; the maximum is 384K.
  - `temperature` accepts 0–2, default 1.
  - `finish_reason` ∈ `stop|length|content_filter|tool_calls|insufficient_system_resource|aborted`.
  - Usage fields are `prompt_tokens`, `completion_tokens`, `prompt_cache_hit_tokens` / `prompt_cache_miss_tokens` and `completion_tokens_details.reasoning_tokens`. The API reference lists the cache fields under `prompt_tokens_details`, but the caching guide shows them at top level. The parser reads both locations plus `cached_tokens`.
  - Rate limits are on concurrency (flash 2500, v4-pro 500 concurrent requests); going over returns 429. While a non-streaming request waits, the server sends blank keep-alive lines. If inference has not started after 10 min, the server closes the connection.
  - Error codes: 400, 401, 402 (insufficient balance), 422, 429, 500, 503.
  - Context caching is automatic and best-effort, matched on whole prefix units.
  - The JSON-output page says nothing about thinking mode or temperature.
- **L2 Request settings**: `{ model, messages, response_format: {type: "json_object"}, max_tokens: <caller>, temperature: 0, thinking: {type: LLM_THINKING ?? "disabled"}, stream: false }`. No `top_p` (default 1).
  - `temperature: 0` is the lowest value in the docs' guidance table (coding/math), which is what PLAN asks for.
  - Thinking is disabled for three reasons. In thinking mode temperature is ignored, so no temperature setting would apply. Reasoning tokens bill as output. And `max_tokens` has to cover the reasoning too, which makes truncated JSON more likely.
- **L3 Live verification (1 call of the ≤ 5 allowed)**: `deepseek-flash` with thinking disabled, json_object and temperature 0 returned valid JSON with `finish_reason: stop`. It took 763 ms and used **98 input + 13 output tokens**, with 0 cache-hit tokens (the field is present). The key came from `.env.local`; EXTRACT_MODEL is unset, so the default was used. The cassette is `fixtures/cassettes/synthetic/llm-smoke.v1/8a9f21a75b7c9bbea951d81c52d1bb3e.json`, and `server/llm/smoke.test.ts` replays it. The dev budget file `.dev/llm-budget.json` now holds 111 tokens under loopId `unset` until the orchestrator runs `pnpm llm:usage --reset --loop <id>`.
- **L4 cassetteKey uses a synchronous pure-JS SHA-256** (`server/llm/sha256.ts`). The §5 signature is sync, `crypto.subtle` is async, and `node:crypto` is not Worker-safe. It is tested against node:crypto.
- **L5 Transport semantics**:
  - The adapter retries `network`, `http_5xx`, `rate_limited` and `empty_content`, backing off 1 s then 3 s. For 429 it honours `retry-after`, capped at 30 s.
  - A retry is skipped, and the last error returned, if its sleep would leave less than 1 s before `deadlineAt`.
  - The per-attempt timer covers the headers and the whole body read, including keep-alive blank lines. An attempt aborted because `deadlineAt - now` was lower than `timeoutMs` returns `deadline`; otherwise it returns `timeout`.
  - `finish_reason` mapping: `insufficient_system_resource` → `http_5xx` (retried); `content_filter` → `http_4xx`; `length` → `truncated`.
  - A non-JSON envelope or a response with no choices → `http_5xx`.
  - One surrounding ```json fence is tolerated before JSON.parse.
- **L6 `LlmError.retryable`** means "the identical request may succeed". It is true for `timeout`, `network`, `http_5xx`, `rate_limited`, `empty_content` and `invalid_json`. It is false for `http_4xx`, `truncated` (the same max_tokens truncates again), `cassette_miss`, `budget_exceeded` and `deadline`. Extract still owns semantic retries (§5).
- **L7 Logging**: every attempt produces one record. That includes outcomes that never reach the provider (`budget_exceeded`, `deadline`, missing key, `cassette_miss`), which get null tokens and latency 0 so refusals can be audited. Failed attempts that the provider billed (for example invalid_json) keep their tokens and count against the budget. A logger that throws never fails the call. Only the error class name is logged, because Drizzle error messages echo bound params, which include raw output.
- **L8 Budget**:
  - The budget is checked before every live/record attempt and fails closed (`budget_exceeded`) when the counter can't be read or is corrupt.
  - Check-then-call is not atomic, so concurrent callers can overshoot by at most their in-flight calls. Accepted.
  - The `fileBudget` limit comes from the file (set on reset), else `LLM_BUDGET_TOKENS`, else 3M. A missing file is created on the first add with loopId `unset`.
  - `d1Budget` sums across all owners, because the limit is per deployment. Its `add()` is a no-op because `d1CallLogger` already writes the rows, so in-app code must always pair the two (`getAppLlm` does).
- **L9 Replay**:
  - No transport retries or backoff: the same cassette always gives the same outcome.
  - `latencyMs` is the recorded latency. If that is larger than the attempt limit (min(timeoutMs, deadlineAt - now)), replay returns `timeout`/`deadline`, so a shorter app timeout reproduces under replay.
  - A cassette that fails the schema, or whose `key` doesn't match its file name, → `cassette_miss` with the reason. Replay never falls back to the network.
- **L10 Record**:
  - Any completion (ok, invalid_json, truncated, empty_content, filtered) overwrites the cassette.
  - Errors without a completion (`timeout`, `network`, `http_5xx`, `rate_limited`) are written only when no completion cassette exists yet, so a transient failure never replaces a good recording.
  - `http_4xx` (key or balance), `budget_exceeded` and `deadline` are never recorded, because they describe the environment rather than the model.
- **L11 JSON-mode prompt assertion**: the messages must contain `/json/i` and a `{` (the example). Otherwise `LlmRequestError` is thrown, except with `NEXTJS_ENV=production`, where a warning is logged instead. The same check validates model, promptVersion, non-empty messages and maxTokens ∈ 1..384000.
- **L12 Missing `DEEPSEEK_API_KEY` in live/record** returns `LlmError{code:'http_4xx'}` rather than throwing, so a misconfigured deployment fails a window instead of crashing the route.
- **L13 Additive API**:
  - Worker safety: Node-only code lives in `server/llm/node/**` and is reached only via `import()`. `misc.test.ts` fails on any static Node import elsewhere in `server/llm`.
  - `LlmError` gains optional `usage`, `finishReason` and `fromCassette`.
  - `createLlmClient` gains optional `fetch`, `now`, `sleep` and `cassetteStore`.
  - New exports:
    - loggers: `memoryCallLogger`, `teeCallLogger`
    - budgets and usage: `memoryBudget`, `sumD1Usage`, `monthStartIso`, `summarizeUsage`
    - cassettes: `memoryCassetteStore`, `dirCassetteStore`, `cassetteDirFor('synthetic'|'real')`
    - CLI and files: `llmModeFromArgs` (`--live` → record, `--live --no-record` → live, `--llm-mode=`), `createCliLlm({mode, runId, runsDir?='eval/runs', cassetteDir?, budgetPath?})`, `readCallLog`, `readFileBudget`, `resetFileBudget`
  - CLI callers load `.env.local` first (core `loadEnvLocal`).
- **L14 Live smoke**: `node --import tsx server/llm/smoke.ts --live` runs in record mode; without the flag it replays. It logs to `.dev/llm/runs/<runId>/` (gitignored) rather than `eval/runs/`, which eval-synthetic owns. The smoke is a script, not a skipped vitest test, because backend critics require zero skipped tests. core-requests/llm.md #2 asks for a `pnpm llm:smoke` alias.
- **L15 `pnpm llm:usage`** adds `--jsonl <llm-calls.jsonl>` (summarize one run) and `--file <budget.json>`. `--remote` runs `wrangler d1 execute xiaoli --remote --json` and is untested until deploy creates the remote DB (deploy critic).

## verify-seed

Round 1 (2026-09-15).

- **V1 Scenario aliases.** The task names `pnpm verify smoke` and `pnpm verify perf`; ARCHITECTURE §8/§12 name `_smoke/home-loads` and `_smoke/perf-budgets`. Both work: `smoke` → `_smoke/home-loads`, `perf` → `_smoke/perf-budgets` (`verify/lib/log-utils.ts SCENARIO_ALIASES`). A directory selector runs every scenario in it.
- **V2 Missing pages never pass a budget.** `_smoke/perf-budgets` reports a budget as `missing: true, passed: false` when the page shows core's placeholder text "这一页还在建设中" or answers 404/501, and when an API answers 501. The run exits 1 until home and person land. A 501 probe the budget already reports as missing is logged in `apiNon2xx` as `expected: true`, so it is not counted twice.
- **V3 Log additions beyond ARCHITECTURE §8** (additive): `description`, `failureReasons`, `abortedRequests` (net::ERR_ABORTED: navigation or React/Next cancelling a fetch; recorded, not failures), `checks` (scenario-declared, a failing check fails the run), `notes`, `budgets[].kind/missing/note`, `pageLoads[].status`, `widths[].trace`. `failedRequests` also includes HTTP ≥ 400 responses for non-API resources (a broken asset would otherwise pass silently). `ExpectedFailure.consoleText` and Chrome's "Failed to load resource … status of N" matched by URL let an error-state step stub a 500 without a console-error failure.
- **V4 Scenario-defined auth.** `account` also accepts `anonymous` (no login) besides `seed|seed2|empty|fresh`. `_smoke/home-loads` clears cookies and signs in through the real form, so the sign-in page itself is covered.
- **V5 Browser code as strings.** `page.evaluate`/`addInitScript` bodies in `verify/lib` are string scripts: tsx (esbuild keepNames) injects `__name(...)` into serialized functions, which throws in the page. Scenario authors hitting "__name is not defined" should do the same.
- **V6 Traces** are recorded per width and kept (`trace-<width>.zip`) only when that width fails; `trace: false` on a scenario (perf) or `--no-trace` turns them off.
- **V7 Seed ids.** Rows get explicit ids starting at `max(id) + 100` per table, computed after the reset, so FK references (evidence → messages, supersede chains, merged persons) are resolved in memory and inserted in ~3,000 multi-row statements (≤ 100 bound params each, 60 per `db.batch`). The gap keeps ids clear of rows the running app inserts meanwhile. Ids stay stable across reseeds only while no other user has higher ids in a table; they can shift, so scenarios must use manifest tags, never literal ids.
- **V8 Seed runtime.** ~22 s end to end locally (getPlatformProxy start, reset 0.4 s, build 0.3 s, insert 19 s, R2 0.05 s). Deletes run child-first, and messages go before handles so D1 does not run 6k `SET NULL` updates.
- **V9 Dataset interpretations.**
  - 200 visible persons (incl. the 2 created by imports) + self (label "我") + 3 merged (hidden) = 204 rows. The pinyin index covers A–Z: Latin labels supply I/U/V, which no Chinese syllable starts with, and "8楼…", "🐱…", "3号…" fill the `#` group.
  - Exactly 5,000 claims. Seeded mix ≈ 79% confirmed / 14% proposed / 4% superseded (reasons `superseded`, `outdated`, `edited`) / 3% rejected, 2% manual (no evidence, no import, confidence null), ~5% sensitive ("提供过收货地址 / 联系方式"; chat text never contains a real-looking address or number).
  - Every AI claim has one "fact" message that states it: first person by the subject, or third person by another member. Evidence is that message plus 0–2 neighbours, so evidence blocks read naturally. ~6,200 messages.
  - **Done imports carry no proposed items.** ARCHITECTURE §6 sets `done` only when no proposed items remain, so proposed items live in `review-mixed`, `failed-windows`, `in-progress` and `delete-me`, and reviewing imports are partly accepted (with review_log `accept` rows, which feed home's "最近有新信息的人"). Status counts: done 8, reviewing 4 (review-mixed, review-empty, failed-windows, delete-me), extracting 1, mapping 1 (`unfinished`, no chat, no R2 staging object).
  - `failed-windows` is status `reviewing` with 2 failed jobs, because the other windows produced items.
  - Mapping-created sender handles (`display_private/display_group`) are `confirmed`, `source_kind 'manual'` with no evidence (the user chose them in step 2). `mentioned`/`address_term` come in pairs from `@显示名 称呼` messages; `real_name` is global (`chat_id` null).
  - Edit history: an edited claim keeps its original `source_kind`. The replacement is a new row with the same kind of evidence, and the old row is `superseded` with reason `edited` (A5 #9).
  - Relation `label` names the `to` person relative to `from` ("林知夏 —妈妈→ X" = X is her mother). Self relations are `from = self`.
  - Attachments: 10 uploaded tiny generated PNGs (`image/png`, R2 `u/<owner>/att/<importId>/<sha256(name)>-<name>`); `uploads-pending` has exactly 3 `selected=1, r2_key=null` images, and that is the only such case in the account, so no other import shows pending uploads. All other images, videos and files have `selected=0`, and some images have a null file name.
  - `delete-me` chat = 40 messages it alone contains. That includes one where self restates a `long-profile` claim, giving that claim evidence from both `delete-me` and `private-long-1`.
  - The leap-month birthday uses the first year ≥ 1987 that has a leap month (1987 → 闰六月). The `lunar-birthday-soon` date is the lunar date of today + 12 days (skipping leap months and day 30).
  - Message `fingerprint` = FNV-1a 64 over UTF-8 of `senderName+sentAt+kind+body`. If the parser's byte encoding differs, seed fingerprints only affect dedup against a re-import of seed chats, which never happens.
  - `parser_version` of seeded imports is `'seed'`.
- **V10 Seed account passwords.** The seed CLI re-hashes the documented password (`better-auth/crypto`) when a scenario changed it, so `xiaoli-seed-2026` always works. Accounts are created via `auth.api.signUpEmail`.
- **V11 Manifest** `.dev/seed-manifest.json`: `{ version, generatedAt, today, accounts: { seed|seed2|empty: { userId, email, seededAt, persons, imports, chats, claims, counts } } }`. `pnpm seed --account X` merges only that account's entry. Tags beyond ARCHITECTURE §9: persons `delete-me-person`, `review-new-person`, `private-alice`, `self`; imports `group-big-1/2`, `owners-1`, `private-long-1/2`, `with-history`, `proposed-heavy-chat`; chats `group-owners`, `group-badminton`, `private-history`, `private-heavy`, `private-in-progress`, `private-failed-windows`, `delete-me`; claims `delete-me-shared`, `history-edited`, `review-change`, `sensitive`.

## eval-synthetic

Round 1 (2026-09-15). Everything in fixtures/synthetic is fictional. Planted details live only in `*.intent.json` (hidden from the annotator until freeze), so they are not repeated here.

- **E1 "basename".** ARCHITECTURE uses the word for both readings; this fixes them.
  - Gold files: the annotator already writes `eval/gold/<source>/<zip file name>.json` (`聊天记录_…_….zip.json`). That is the preferred name: gold-template writes it, and eval also accepts `<name without .zip>.json`.
  - Intent files: `fixtures/synthetic/<name without .zip>.intent.json`.
  - Synthetic LOCK key: the name without `.zip`.
  - The gold `zip` field holds the full file name including `.zip`.
- **E2 Synthetic file names follow the real pattern `聊天记录_YYYYMMDD_HHMMSS.zip`**, so the parser's `exportedAt` path is exercised. `fixtures/synthetic/README.md` maps files to chats and purposes without trap details. The perf file keeps the ARCHITECTURE name `perf-5000.zip`. The eval set has 4 ZIPs: one private chat exported twice (207 and 204 messages) whose exports share exactly 32 messages, 2026-03-22 10:05 to 2026-04-05 21:45 (14 days; corrected in round 2, round 1 said 16 days), a parents' group (178) and a family group (215). The README purpose column is generated: chat type plus the overlap computed from the laid-out messages, never a hand-written window.
- **E3 Guessed formats for SPEC §6 "未见样本"** (labelled as guesses in the generator and in `intent.guessedFormats`):
  - quote: `[引用] 名：原文\n回复`, and `「名：原文」\n- - - …\n回复`
  - recall: `"名"撤回了一条消息` / `你撤回了一条消息`
  - contact card: `[名片] 名`
  - location: `[位置] 地点`
  - link: `[链接] 标题 URL`
  - file: `[文件] 文件名` (the file sits in the media folder)
  - forward: `[聊天记录] …的聊天记录`
  - system: `"A"邀请"B"加入了群聊`, `"A"修改群名为"…"`, `"A" 拍了拍 "B"`, sent under the actor's display name
  - `[群公告] …` and `[音乐] …`: unknown
  - empty image name: `[图片]`
  - The parser (`wechat-export@1`) classifies all of these exactly as declared; `eval/tests/parser-synthetic.test.ts` asserts the split and the SPEC kinds.
- **E4 Relation direction for gold: `from` is the `type` of `to`** (from = parent, to = child for `parent`). The matcher accepts the reverse only with the inverse type (parent↔child) or for symmetric types. ARCHITECTURE does not say this, so core request eval-synthetic #2 asks for it; extract's prompt must use the same reading.
- **E5 Filler.** Chats mix hand-written scenes (facts, traps, edge cases) with everyday filler dialogues from fixed pools (deterministic PRNG). Filler is meant to carry no lasting facts, but gold is annotated independently and is authoritative, not intent.json. Local check against the real exports, run once and printing counts only: none of the synthetic person or place names occur there; only two generic address terms that SPEC itself quotes do.
- **E16 Filler realism (round 2).** Round-1 filler repeated dialogues up to 10x per chat and ignored clock, weekday, season and roles. Each filler dialogue now declares a start-time window, an optional calendar predicate (workday/holiday, month, school term from 2026-09-01) and speaker tags per role (family: elder/adult/student; parents' group: parent/grandma). A dialogue is used at most twice per chat (`once` for things that cannot plausibly happen twice, e.g. the same workbook page), repeats are ≥ 30/6/21 days apart (private/parents/family) and started by a different speaker. Non-ack text bodies are unique within a pool. `eval/tests/synthetic.test.ts` asserts per ZIP: no text body of ≥ 4 chars more than 2x (acks 收到/好的/谢谢/哈… exempt), no 晚安 before 20:00, no body starting with 早 at/after 11:00, no 降温/银杏大道 in May–Aug. Filler counts dropped (parents 45→34 dialogues, family 55→42), which also lowers message-count inflation for yieldPer100.
- **E17 Text-file edge cases (round 2).** `聊天记录_20260912_095501.zip` (family group) is written with a UTF-8 BOM, CRLF line endings and no final newline (`ExportDef.text`). Recorded in its `intent.textFile` and `intent.edgeCases.bom|crlf|noTrailingNewline`; the other eval ZIPs and perf-5000 stay plain LF with a trailing newline. The parser test compares the parser's split with the §6 splitter on this ZIP too. Chosen over perf-5000 so import/verify scenarios that use eval ZIPs exercise it; gold was not frozen for any synthetic ZIP, so the bytes could still change.
- **E18 Blind annotator docs (round 2).** GOLD_FORMAT.md examples are invented (a badminton club) and gold-schema.ts's relation comment uses 外公/外孙; neither appears in any fixture. A test fails if any planted text/value/label, negative description/forbidden or sensitive value from `*.intent.json` appears in GOLD_FORMAT.md, gold-schema.ts or fixtures/synthetic/README.md.
- **E6 Entries before extract exists.** `eval/src/entries.ts` copies the §1.2/§5/§6 signatures and loads `lib/wechat-export/index.ts`, `server/llm/index.ts` and `server/extract/index.ts` by dynamic import, so eval compiles in wave 1. `pnpm eval` exits 2 naming the missing module, and exits 3 when no selected ZIP has gold. Swap the copied types for `import type` from the public entries once extract lands.
- **E7 Gate details beyond §7.4.**
  - A precision gate with 0 predictions passes, value null with a note; so does a recall gate with 0 required gold. A source with no scored zips fails.
  - Added gates: `zips.scored ≥ 1`, and `zips.unscoredWithGold == 0`, so a gold_mismatch, schema-invalid gold or extractOffline crash fails that source, as §7.2 requires for drift.
  - `p95WindowMs` is informational unless the mode is record/live with deadlinePolicy app.
- **E8 `passed` requires both sources.** A `--source synthetic` run reports real gates as "not evaluated" and `passed: false`. The report adds `passedBySource`.
- **E9 Judge call economy.**
  - Step A runs only for persons that have both gold and predicted claims (other cases are trivially FN or FP), and once per ZIP for events, with no participant comparison.
  - Predictions are chunked by 12 per call, because §7.3 fixes maxTokens at 1024.
  - The one-to-one assignment happens in code: a `same` pass, then a `less_specific` pass, both in gold order, lowest prediction index first.
  - Duplicates of an already-matched fact become FPs, which the judge labels.
  - The judge in live/record uses a non-recording LLM client; its answers are kept in `eval/judge-cache/<source>/`, not in cassettes.
- **E10 Deterministic relation `wrong_person`.** A predicted relation labelled this way shares an endpoint and a compatible type with some gold relation without matching it. An inference-trap relation next to a real one can therefore be labelled wrong_person rather than over_inference. The precision gate is unaffected.
- **E11 Harness sensitive detector** (independent of extract's guard):
  - spaces and hyphens between digits are removed before the phone / ID / bank patterns, so card numbers written in groups are caught;
  - an address needs digits next to 号/栋/幢/单元/室/弄 (not after 月, so "10月3号" is a date), or 路/街/巷 + digits + 号;
  - `sensitiveValues` match ignoring whitespace.
- **E12 Real data handling in tools.**
  - The combined report drops `problem` strings and renames real ZIPs `real-1..n` (also inside warnings).
  - Real detail reports, annotation views, gold templates and validate-gold detail files are written only after `git check-ignore` confirms the path; otherwise the command refuses.
  - validate-gold prints issue text for synthetic only. For real data it writes `.dev/annotate/real/validate-<lockKey>.txt` and prints that path.
- **E13 `evidenceOverlap`** = lenient TPs of all types whose evidence intersects the gold evidence, divided by all lenient TPs.
- **E14 Compare.** `pnpm eval --compare vA` runs vA and the current prompt in one invocation, writes both reports and `compare-vA-vs-vB-<ts>.json`. `tsx eval/src/cli.ts compare A.json B.json` compares two saved reports and refuses when gold hashes differ.
- **E15 Report file timestamps** are UTC `YYYYMMDD-HHmmss`. Run ids are `<ts>-<promptVersion>` unless `--run-id` is given.

## integrator

Wave 1 integration (2026-09-15).

- **I1 Core requests.** All open requests accepted and done: eval-synthetic #1 (`synthetic:generate` script), #2 (relation direction in ARCHITECTURE §2.5/§7.2), #3 (annotator may read GOLD_FORMAT.md, gold-schema.ts, fixtures/synthetic/README.md; §2.5 label example 老爸 → 外公); llm #1 (`fakeLlm`/`createTestApp.llm` typed with `LlmClient`, type-only import; `AppDeps.llmOverride` stays `unknown` so core has no runtime dependency on llm), #2 (`llm:smoke` script); verify-seed #1 (`test:e2e` script). Module CLIs added to package.json go through `scripts/run-owned.ts` like the existing ones.
- **I2 Relation direction and label (seam fix in a module file).** Contract: a relation reads `from` 是 `to` 的 `type`, and `label` names `from` as seen from `to` (from=外公, to=外孙, type=parent, label=外公). The seed stored the reverse (its chat line "X是我妈妈" became from=speaker, to=X, type=parent, label=妈妈). Integrator swapped the stored endpoints in `scripts/seed/dataset.ts` `relation()` (one line; the generated chat text is unchanged) and changed the self-relation assertion in `scripts/seed/seed.test.ts` from `fromPersonId = self` to `toPersonId = self`. So verify-seed V9's "Self relations are from = self" now reads "to = self", and "林知夏 —妈妈→ X" is stored as X —parent/妈妈→ 林知夏. person (wave 3) derives relationToMe from relations with self on either side; extract's prompt must use this reading.
- **I3 Gold validation.** Integrator does not edit gold (annotator-owned). Wave-1 run of `pnpm eval:validate-gold`: synthetic 4/4 pass; real 1/3 pass (1 warning: annotated before the parser existed), 2/3 fail on schema errors (null `forbidden` where the schema expects the field omitted; one negative description over 200 chars). Nothing is frozen yet. Handed to the annotator at the start of wave 2.
- **I4 Pre-commit real-name scan (seam fix in a module file).** The staged-content check (sender display names of length ≥ 2 parsed from the real exports, counts only) found one hit: a common Latin first name that verify-seed had picked as a synthetic `SPECIAL_LABELS` entry equals a real sender's display name. Integrator renamed it in `scripts/seed/names.ts` (label now `Nora Chen`, key `T.privateLatin`) and `scripts/seed/dataset.ts` (2 references). The manifest tag string is unchanged, so scenarios are unaffected. Future builders should run the same scan before handing off seed or synthetic names.

Wave 2 core-request round (2026-09-15, triggered by extract-r1-before-critic).

- **I5 Core requests (wave 2).** extract #1/#2 were already done. import #1: `unwrap` now takes the whole RPC response union (`R extends ClientResponse<unknown, number, ResponseFormat>`) and infers the success body by distributing over it, so validated routes (extra 400 branch) unwrap without casts; single-response callers are unchanged. import #2: `--color-ink-inverse` added to `@theme inline`. import #3: ARCHITECTURE §1.4/§11 text brought in line with import's implementation (DECISIONS import I1–I4): optional `existing[].sentAt` in `alignMessages`, rescale before inserts, chat linked before job creation, `parsed` = mapping in flight, delete-import removes the import's unused manual display handles. Contract text only; no schema or API shape changed.

Wave 2 integration (2026-09-15, end of wave 2).

- **I6 Core requests (end of wave 2).** extract #3 and import #4 already had their ARCHITECTURE text applied; status set to done. extract #4: ARCHITECTURE §6 `validateOutput` drop reasons add `low_confidence | momentary | ambiguous_handle | redundant`, a "Round-3 rules" sentence records X26 (group-chat vocative handles, 0.85 confidence floor, momentary statements, redundant student status, `other` label containing a name, 儿子/女儿/孩子 claims moved to the child), and §7.5 notes the extract-owned top-up eval `server/extract/scripts/eval-fill.ts` (X27). Text only; no schema, contract or route change. No request is open.
- **I7 Seams checked (no module file changed).** search S9 (picker/search "新建人物" was only verified against a stubbed `POST /api/people`): a probe against the real route shows review's handler returns 201 `{ person }` as `createPerson` in `components/search-overlay/lib/fetchers.ts` expects, the new person is found by `GET /api/search?types=people`, and an empty label is 400. import → extract (`processNextJob`, `retryFailedJobs`, `createJobsForImport` after chat link, I3) and import/review/search routes use only public entries (`@/server/extract|review|search|llm`). Remaining 501 routes are wave-3 (`home`, `chats`, `people`, `export`). The `as unknown as ClientResponse` casts in search `fetchers.ts` and review `evidence.tsx` and import's `requestJson` helper are no longer needed after import#1 but still compile; left to their owners.
- **I8 Wave-2 checks.** `pnpm typecheck` clean; `pnpm test` 46 files / 425 tests passed, 0 skipped; `pnpm db:migrate:local` no pending migrations; `pnpm seed` ok; `pnpm verify` `_smoke/home-loads`, `import/showcase`, `import/flow`, `review/evidence-showcase`, `search/showcase`, `search/person-picker` all pass (0 console errors, 0 failed requests). Gold: 7/7 frozen (LOCK v2), committed with this wave, so the eval's `lockAppendOnly` has a baseline from now on. Extract eval `eval/reports/20260915-123434.json` (extract.v5, replay) passes every gate for both sources; events recall (0.08 synthetic) is not gated and stays a known weakness.
- **I9 Pre-commit real-name scan (seam fix in a module file).** The staged-content scan (sender display names of length ≥ 2 from the real exports, 11-digit strings; counts only) found 0 numbers and 1 name: a common Latin first name that equals a real sender's display name appeared inside a test-client identifier (`as` + name) in `server/import/imports.test.ts`. Integrator renamed the identifier to `asUserA` (36 occurrences; no behaviour change, the file's tests still pass). Same class as I4: builders should use neutral identifiers such as `userA`/`userB` in tests.

## search

Round 1 (2026-09-15).

- **S1 People matching.** Query and stored text use the same normalization as `value_norm`/`statement_norm` (NFKC, lowercase, trim; inner whitespace collapsed). Candidates: every person of the owner with `merged_into_id` null and `is_self` false (self is never a search result; PersonPicker offers "我" via `allowSelf`). Label is matched in JS over the owner's visible persons (≈200 rows); aliases via SQL `value_norm LIKE %q% ESCAPE '\'` on handles with status `confirmed` or `proposed` (rejected/superseded aliases never match). Tiers: label exact 100 > alias exact 90 > label prefix 80 > alias prefix 70 > pinyin initials exact 66 > full-pinyin prefix (≥ 2 letters) 62 > initials prefix 58 > label contains 50 > alias contains 40; ties by pinned, then latest `last_message_at`, then `label_sort`. `matchedAlias` is set only when an alias scored higher than the label (an alias equal to the label is ignored).
- **S2 Pinyin.** Only for queries of ASCII letters (spaces allowed), only against labels containing Han characters, derived from the stored `label_sort` (fallback `sortKey(label)`): "lzx" / "linzhi" → 林知夏. Aliases are not pinyin-matched (they are often nicknames; would add noise). Latin labels match through the normal label rules.
- **S3 Claims.** Confirmed claims only, merged persons excluded, owner-scoped on both claims and the joined person. Every whitespace-separated term must appear in the statement (AND). With several terms, a term that matches a person (S1 rules) may instead select that person: "林知夏 茶具" → 林知夏's claims containing 茶具; those hits come first, then plain AND hits. Order: shorter statement first (more specific), then newest id. `highlights` are `[start, end)` UTF-16 offsets into the original `statement`, computed per code point so NFKC width changes (ＡＢＣ) still map back; overlapping ranges merged. `limit` (default 20) applies per group. No FTS (A6); on the seed account `GET /api/search` measures well under 300 ms (see verify log).
- **S4 People index.** `GET /api/people` (the `index` param is optional; pinyin is the only index) = visible persons (not self, not merged), grouped by `lib/pinyin.indexLetter(label)` A–Z then `#`, within a group by `label_sort` (fallback `sortKey`), then label, then id. `total` = number of persons listed.
- **S5 Overlay behaviour.** Instant search debounced 120 ms with previous results kept while fetching (dimmed). IME: the query only updates on `compositionend`, and Enter/arrows during composition are ignored. Enter pressed before the latest results arrive is applied once they arrive (never opens a stale row). Under 640 px the overlay is full screen with a "取消" text button; desktop footer shows ↑↓ / Enter / Esc hints and no result counts (SPEC §9.3). Claim result → `personHref(personId, {type:'claim', id})`; when the target path is the current path, navigation sets `location.hash` so the person page receives `hashchange` (Next's router does not fire it for hash-only pushes). "新建人物『q』" posts `POST /api/people {label: q.slice(0, 60)}`, invalidates `peopleIndex`, `home`, `['person']`, `['search']`, then navigates; errors show inline under the row.
- **S6 PersonPicker behaviour.** Empty query: "我" (if `allowSelf`), candidates in order with reason, "新建人物『defaultLabel』" (if `allowCreate`). With a query: "我" only while the query is a prefix of 我/自己/wo, candidates whose label contains the query, then search results minus `excludeIds` and minus candidates (alias hits show "又名 X" as reason), then "新建人物『query』". First Esc clears the query (stopPropagation), the next closes the popover/dialog. States inline: "正在查找…", "没有找到", "没有查到 · 重试".
- **S7 Shared pure text.** `server/search/text.ts` has no server imports and is also imported by the overlay and picker, so client-side label/alias highlighting equals the server's matching.
- **S8 Keyboard tests without a DOM test stack.** The repo has no jsdom/testing-library; rather than request dependencies, keyboard logic is pure (`components/search-overlay/lib/keys.ts`, unit-tested: ⌘K/Ctrl K toggle, "/" only outside text fields, IME, wrap-around) and the real keyboard flows run in `verify search/showcase` and `search/person-picker` checks (↓↑ Enter to `#claim-`, "/" typed into a field does not open, Esc, picker Enter/Esc).
- **S9 Showcase stubs `POST /api/people`.** In wave 2 that route is review's 501 stub; the scenario fulfils it with 201 to verify the UI flow (Enter → navigate to the person) without adding a person to the seed account. The real round trip is to be re-checked once review's route lands.

## review

Round 1 (2026-09-15). Public entries `@/server/review` and `@/components/evidence`; contract shapes unchanged.

- **R1 Supersede on confirm.** `accept` (single, bulk, or `edit` of a proposed claim) of a claim with `supersedesClaimId` marks that older claim `superseded` / `status_reason 'superseded'` / `superseded_by_claim_id = new`, if it is confirmed or proposed. Rejecting a confirmed claim that had superseded another restores the older one to confirmed. Superseded rows are read-only: accept/reject/edit → 409 `conflict` (bulk reports `code: 'conflict'`).
- **R2 Edit.** Proposed/rejected claim: edited in place and confirmed (reviewLog `before`). Confirmed claim: new confirmed row (copies evidence, source kind, import id, confidence; `supersedes_claim_id = old`), old row `superseded/edited` (A5 #9). Handles/relations/events/dates are edited in place and confirmed, with no history. Edit with no effective fields → 400. Mentions (`claim_mentions`) are recomputed from other active persons' labels (≥ 2 chars) found in the statement, for edits, replacements and manual claims.
- **R3 Supersede ("已过时").** Claims only (other types → 400); only confirmed (→ 409). Sets `validTo` = today in APP_TZ, reason `outdated`; `replacement` creates a manual confirmed claim, same category, `supersedes_claim_id = old`, and old `superseded_by_claim_id` points to it.
- **R4 Delete item.** Hard delete of item + evidence + claim_mentions/event_participants. A claim it had superseded (reason `superseded`) becomes confirmed again; edited/outdated originals only lose the pointer. Deleting a handle nulls `messages.sender_handle_id` (raw `sender_name` stays).
- **R5 reviewLog for actions without an item target type.** `review_log.target_type` is TargetType (no `person`), so merge writes one `merge` row per moved handle/claim/date/event/relation, split writes one `split` row for the handle plus one per candidate item, and manual adds write `edit` with `before: null, after: { created: 'manual', … }`. `POST /api/people` writes no log. Not worth a contract change.
- **R6 Import status.** After any review action the affected imports move `reviewing → done` when none of their items is `proposed`, and `done → reviewing` when some are again (split candidates). Imports in other statuses are untouched.
- **R7 Import review grouping.** Items = rows with `import_id = :id` and `source_kind 'ai'`, all statuses (handled items stay in place), except originals of a person-page edit (`status_reason 'edited'`). "变化" = claims whose `supersedesClaimId` points to a claim that is not their own edit original. Relations/events touch several persons: section = a non-self endpoint created by this import, else one that already has a section, else the first non-self. `newCount` counts all of the section's items in this import (not only proposed) so sections do not reorder while the user reviews. Sort within groups: claims by category order work→other, others by id; handles before relations.
- **R8 Evidence segments.** Evidence messages ±context in the same chat. Windows in one chat merge into a single `EvidenceItemDTO` only when they share a message or the second starts at the chat message right after the first window ends (round 2, see R14) (several `isEvidence` messages; `messageId` = first evidence message), so a message never repeats. Segments are ordered by the first evidence message's time. Contract allows it (per-message `isEvidence`).
- **R9 Merge details.** `from` self → 409; either side merged → 409; same id → 400; merging into self is allowed. Relations: endpoints remapped; self-loops deleted with their evidence; exact duplicates `(from, to, type)` fold into the existing relation (evidence union, confirmed wins). `from.pinned` carries over to `into`; persons merged into `from` are re-pointed to `into`.
- **R10 Split details.** Items of the original person with ≥ 1 evidence row whose messages were all sent by the split handle are candidates; confirmed ones go back to `proposed` (proposed ones stay and are listed); superseded/rejected are skipped. `last_message_at` is recomputed for both persons. The new person has no `import_id`.
- **R11 Manual relation direction.** `POST /api/people/:id/relations { toPersonId, type, label }` stores `from = :id`, `to = toPersonId` in the §2.5 reading (`from` 是 `to` 的 `type`). Person UI must phrase its form accordingly. Manual events always include `:id` as a participant.
- **R12 Evidence UI.** The block sits directly under the row (for `as="p"` it is rendered as the paragraph's next sibling, because a `<div>` inside `<p>` is invalid HTML). Time: the first message of a segment and any message on a new day use `formatMsgTime 'full'`, the rest `'short'`, so repeated dates don't crowd the line (deviation from "full" on every line in §2.8). Chips: voice "语音 N 秒", transfer "转账" + state text, red packet "红包" + greeting, video call "视频通话" + duration, and also image/video/动画表情/小程序/视频号/文件/链接/位置/名片/聊天记录, followed by their visible title text. An AI item with no remaining evidence shows "原话已经不在了". The error message is "原话没有加载出来 · 重试", shown while the row stays intact. A refetch after an error shows the skeleton.
- **R13 Showcase.** `/dev/review/evidence` uses synthetic fixtures in a local QueryClient (staleTime ∞, retryOnMount off): data seeded with `setQueryData`, loading = a never-settling fetch, error = a query already in the error state, so no network calls are involved. `?claim=&claim2=` adds rows backed by the real API for seed claims; the scenario uses them for loading (delayed route), 500 error, and retry.
- **R14 Evidence adjacency (round 2).** Chat `seq` has gaps (steps of 1024), so adjacency is not a seq difference or a position in the fetched list. Each window fetches `ctx + 1` messages after the evidence message; the extra one (not shown) is the window's chat successor. Two windows merge iff they overlap by seq or the next window's first message is that successor. Far-apart evidence therefore gets its own segment and its own "在聊天中查看" link.
- **R15 D1 parameter cap (round 2).** Multi-row inserts are chunked by column count (`rowsPerInsert(cols) = ⌊96/cols⌋`: link tables 24 rows, evidence 19, review_log 12). Batches hold ≤ 100 statements (ARCHITECTURE §4.2).
- **R16 Bulk plans against evolving in-memory state (round 2).** Planning an item updates the loaded rows (status, supersede pointers, restores), and a claim appears as one shared object across the item list and its related claims. A claim that an earlier item in the same request supersedes is reported `conflict` and stays `superseded`. Listing the old claim first confirms it, then the replacement supersedes it; statements run in item order.
- **R17 Undo instead of transactions (round 2).** Rows that must exist before the batch (the new claim for an edit of a confirmed claim or for a replacement, the new person for a split, manual items) are removed by an idempotent undo if the batch fails; the undo also restores the original claim or the handle's person. In a merge, the person rows (`mergedIntoId`) come last, after the review_log rows, so if a merge longer than one batch is cut off it can be run again.
- **R18 Import review counts only shown items (round 2).** A handle without a person and an event without participants have no section, so `allHandled`/`empty` ignore them.
- **R19 Evidence UI details (round 2).** On narrow screens (< 640px) the mark has an invisible hit area of about 27×27 px (`[data-evidence-hit]`); the glyph itself is unchanged. Inside the bordered block, BlockError drops its own left rule. For element shots in the showcase scenario, the top bar is switched to non-sticky just for the shot.

## extract

Round 1 (2026-09-15). Public entry `@/server/extract` has the ARCHITECTURE §6 signatures plus additive exports (`d1Store`, `memoryStore`, `getProgress`, `applySensitiveGuard`, `detectSensitive`, `renderDedupPrompt`, `getPrompt`, `listPromptVersions`, `DEDUP_PROMPT_VERSION`).

- **X1 Prompts are bundled, not read at runtime.** Workers have no fs. `server/extract/scripts/gen-prompts.ts` compiles every `prompts/*.md` (front-matter `version`, sections `system` / `user_template` / `example_json`) into `server/extract/prompts.generated.ts`. `prompts.test.ts` fails when that file is stale or a version differs from its file name. Old versions stay in the registry, so `pnpm eval --prompt/--compare extract.vN` keeps working.
- **X2 Windowing.** A gap of more than 3 h starts a new session (exactly 3 h does not). Inside a session, new messages are clustered when at most 40 existing messages separate them. Each cluster gets up to 20 existing messages on each side (never across a session boundary). The span is then cut into windows of at most 150 messages, overlapping by 20. Windows without a new message are not created. The last window can be short (151 messages → 150 + 21); it is not rebalanced.
- **X3 Context messages (SPEC §8.4).** In-app, a message is context when its `first_import_id` is not the job's import (additive `WindowMessage.context`). The prompt marks those lines 【上下文】. Validation drops items whose evidence is only context messages (`context_only`); context evidence next to a new message is kept. Offline runs have no context (focus = whole export).
- **X4 Strict validation per item, not per window.** The top level stays strict: a non-object, an unknown key or a non-array value is `validation_failed` and retried. Each array item is parsed with its strict zod element schema, and an invalid item is dropped (`invalid_item`). At temperature 0 a retry would reproduce the same bad item, so failing the whole window would lose every good item after 3 identical attempts. Before the strict parse: null optional fields are removed, strings trimmed, evidence `"#12"`/`"12"` → 12, bare ids/strings in person positions → PersonRef. Additive drop reasons: `invalid_item`, `context_only`, `self_loop`.
- **X5 Item rules.**
  - An evidence seq outside 1..n drops the item (SPEC §8.6).
  - An unknown `supersedesClaimId` is stripped; the claim is kept.
  - A `newPersons` entry whose label equals a known person's label or handle becomes that person.
  - A handle equal to a name the person already has is dropped. Sender display names come from mapping, and GOLD_FORMAT.md says they are not handles.
  - Exact duplicates within a window merge their evidence. Relation types are lowercased. Events keep only known participants.
  - `newPersons[].evidence` is not gated (person rows have no evidence). A tempId that no surviving item references creates no person.
- **X6 Sensitive guard** (deterministic, after validation).
  - Patterns are at least as strict as the harness detector: phone incl. landline, ID and bank card numbers after joining digit groups; digits next to 号/栋/幢/单元/室/弄 (not after 月); 路/街/巷 + digits + 号.
  - A bare day number ("5号") becomes "5日" when the text has no address words, so dates are not mistaken for house numbers.
  - Claim rewrites: 提供过联系电话 / 提供过证件号码 / 提供过银行卡号 / 提供过收货地址 (text mentions 收货/快递/寄/邮) or 提供过详细住址, with `sensitive: true`.
  - Sensitive handles are dropped. An event with a sensitive summary is dropped. Sensitive places and relation labels are removed.
- **X7 tempId resolution across windows.** A new person is reused when this import already created a person with the same normalized label. It is also reused when that person's label contains, or is contained in, the new label (≥ 2 chars) and it already has evidence on one of the same messages. Otherwise it is created (`persons.import_id` = this import, `label_sort` via lib/pinyin).
- **X8 Dedup.** Exact normalized statement matches merge without a model call. The remaining new claims go into one `dedup.v1` call per window, grouped by person. Candidates are this import's proposed/confirmed claims plus confirmed claims overall for that person, last 80. Entries naming an index or id outside their group are ignored. A failed dedup call counts as "no duplicates" (`dedup: 'failed'`).
- **X9 Persistence uses raw SQL on `D1Database.batch`.** drizzle 0.45's D1 `batch()` throws a runtime TypeError on `db.run(sql)` items, although they typecheck. Statements are therefore compiled with drizzle's `SQLiteSyncDialect` and sent in batches of ≤ 90 via `$client.batch(prepare().bind())`.
  - Each item and its evidence / mentions / participants are in the same batch, which D1 runs as one transaction. A new claim's id is read inside the batch as `max(id)` over the owner's claims; relations, dates, events and handles are found by natural key.
  - Handles: `INSERT OR IGNORE` on the unique index. Evidence attaches only when the existing handle belongs to the same person.
  - A relation is not inserted again when the same or the inverse relation exists in any status (parent↔child, service_provider↔client, symmetric types either way). Dates are matched by (person, kind, calendar, month, day), events by (import, summary).
  - Evidence rows only reference messages of the import's chat. `itemsCreated` counts rows actually inserted; evidence merged by dedup is not an item.
- **X10 Supersede.** Extract only stores `supersedes_claim_id` on the proposed claim. The old confirmed claim stays confirmed until review confirms the new one; review owns supersede-on-confirm (§1.6).
- **X11 Jobs.**
  - Claiming increments `attempts`, so an attempt counts even when its request dies. A job `running` for more than 60 s can be claimed again; claiming it after its 3rd attempt marks it `failed` with code `deadline`. The completion update is guarded by `locked_at`.
  - Retried codes: `invalid_json`, `truncated`, `timeout`, `deadline`, `validation_failed`, `llm_error` (http, network, empty content) and `internal` (storage exception; only the error class name is logged). `budget_exceeded` and `cassette_miss` fail the job at once.
  - With nothing pending or running, the import becomes `reviewing` when failed windows or proposed items exist, else `done`.
  - `processNextJob` on an import that is not `extracting` returns `processed: null` and changes nothing. `retryFailedJobs` puts the import back to `extracting`. `createJobsForImport` is idempotent (returns 0 when jobs exist).
- **X12 Deadline clock.** No model call is made when the extract timeout would be under 1 s; the attempt counts as `deadline`. The attempt clock replaces wall time spent inside LLM calls with each call's reported latency. So recorded latency counts against the 28 s in replay exactly as in live, and the "≥ 4 s left for dedup" check uses the same clock.
- **X13 Offline merges across windows.** When a later window's item merges into one created earlier, only evidence inside the earlier item's window is attached, because the harness checks each item's evidence against its `windowIndex` (`invalidEvidencePost`). In-app there is no such restriction.
- **X14 Prompt input in-app.**
  - Known persons: everyone with a handle scoped to the chat, the window's senders and self, with merged persons resolved to their target. Each person gets up to 12 handles and the latest 40 confirmed claims.
  - Image/video bodies render as `[图片]`/`[视频]` (generated file names are noise); other kinds keep the parser's body.
  - No self person yet → self id 0, and items about self are dropped.
- **X15 Failure cassettes** are hand-written in `server/extract/__fixtures__/cassettes/extract.v1/`: ok, invalid JSON, truncated (`finish_reason: length`), timeout, schema-invalid, empty content. Responses are written by hand; the request and key are computed from the real rendering by `__fixtures__/build-cassettes.ts`. `cassettes.test.ts` fails when they are stale and replays each through the real llm adapter (3 attempts, then failed).
- **X16 CLI.** `pnpm extract:offline <zip> --gold <file> | --mapping <file> [--live]` reads only `mapping` from a gold file. Results for real exports go to `.dev/extract/offline/` with counts on stdout; call logs go to `.dev/extract/runs/`.
- **X17 Model behaviour, extract.v1** (live eval `20260915-110648`, deepseek-flash, thinking disabled, temperature 0, deadline app; aggregate numbers only).
  - Synthetic: claims P 0.575 / R 0.72, handles P 0.82, relations P 0.79, transactional 0.15.
  - Real (tiny n): claims P 0.71 / R 0.33, handles P 0.50, relations P 0.50.
  - Sensitive in statement 0, invalid evidence post 0 / pre-filter rate 0.00. p95 window ≈ 2.1 s. JSON was always valid, with no truncation or timeouts. The model writes little per window (~70 output tokens on average).
  - FP patterns:
    - one instance, joke or compliment turned into a habit or trait;
    - short-lived schedules and states recorded (trips, interview rounds, last working day, minor illness);
    - facts about a named child or spouse attached to the parent;
    - grandparents typed `parent`;
    - `service_provider` pointing at the wrong person;
    - an address term given to the speaker instead of the person addressed;
    - a surname alone as `real_name`;
    - a lunar birthday emitted as solar.
  - FN patterns: `@name` handles, full real names, child and grandparent relations, and the generic "提供过…" claim when someone posts an address or account were rarely produced.
  - Usage: ~0.52 M tokens for one full run (most input is the system prompt repeated over many small windows, because synthetic chats have many short sessions).
- **X18 extract.v2** addresses X17 with generic rules and invented examples only:
  - assign facts to the person they are about (create named family members);
  - `relative` for non-direct kin; `service_provider` `to` = the person served;
  - address terms belong to the person addressed;
  - `real_name` only as a full name;
  - no habits from a single instance;
  - no short-term plans or states;
  - keep the lunar calendar as stated;
  - emit "提供过收货地址 / 联系方式 / 银行账户", one per kind.
  - The guard's generic texts changed to 提供过联系方式 / 提供过银行账户 (SPEC §3 "银行信息"; GOLD_FORMAT.md's wording).
  - Result (`20260915-111345`, compare file `eval/reports/compare-extract.v1-vs-extract.v2-20260915-111713.json`):
    - synthetic: claims P 0.78 / R 0.72, handles P 1.00, relations P 0.90, transactional 0.11;
    - real: claims P 1.00 / R 0.33 (2 predictions), handles P 1.00, relations P 0.50 (2 predictions).
  - Still over: precise days inside statements ("最后一天是…", start dates), which the model also converts wrongly from relative days. The v2 instruction to put stated times into statements was too broad.
  - Other residue: restatements of an earlier fact with extra detail survive `dedup.v1`; a passing "你儿子" creates a person and relation; one label/direction flip.
  - Usage ≈ 0.64 M tokens.
- **X19 extract.v3 + dedup.v2** (live eval `20260915-111858`; compare file `eval/reports/compare-extract.v2-vs-extract.v3-*.json`).
  - Prompt changes: statements hold only the lasting fact (a year or month may stay for explicitly dated life milestones; no start dates, last days or relative days converted to dates); plans are recorded as outcome states; a person is created only when named or addressed and something specific is known about them; a direction self-check sentence for relations. `dedup.v2` treats the same fact with more or less detail as a duplicate, while a new change is not.
  - Result:
    - synthetic: claims P 0.86 (passes) / R 0.68 (gate 0.70), handles P 0.92, relations P 0.89 (gate 0.90), dates 0.60, transactional 0.06 (gate 0.05);
    - real: claims P 1.00 / R 0.33 (3 gold claims), handles P 1.00, relations P 1.00, dates 1.00.
    - Sensitive 0 and invalid evidence 0 in all three runs. p95 window ≈ 2 s.
  - Round 1 stops live calls here. The loop total is ≈ 1.83 M of 3 M (`pnpm llm:usage`); one more full run (≈ 0.67 M) would cut into the ≥ 600 k reserved for critics. The critic can replay v3 from `fixtures/cassettes/{synthetic,real}/extract.v3` and `dedup.v2`.
  - Open for round 2:
    - synthetic claims recall (education facts of non-sender children, a school/major split into separate facts);
    - `@name` mention handles still rarely produced;
    - transactional residue on job-change timing;
    - one relation FP away from the relations gate;
    - real claims recall on a 3-item gold (one service-provider claim/relation toward self, one "is a student" claim).

Round 2 (2026-09-15).

- **X20 Window packing and gap markers, bound to the prompt version.**
  - `promptFeatures(version)` in prompt-version.ts turns packing on from `extract.v4`.
  - `packWindows` merges consecutive plans into one window only when the second starts at the message right after the first ends and the merged span holds ≤ 40 messages. It is used by `createJobsForImport` and `extractOffline`.
  - Unchanged: sessions (> 3 h), the 150/20 split, ±20 context. Overlapping and non-adjacent plans are never merged.
  - Messages more than 3 h apart get a `—— 间隔约N小时/天，以下是新的一段对话 ——` line.
  - `extract.v1–v3` keep one window per plan and the old rendering. Replaying v3 with the new code reproduces `20260915-112641` exactly (0 misses, 0 fallback).
  - Why: synthetic chats split into many tiny sessions. v3 made 180 extract calls for 804 messages (median 4 messages per call), and about 95 % of input tokens were the repeated system prompt. A full eval cost ≈ 0.65 M tokens, more than the loop had left after round 1.
  - Packed runs make 27 extract calls plus about 13 dedup calls. A full record eval including the judge costs ≈ 0.15–0.19 M tokens.
  - Recorded p95 window times: v4 3.7 s / 1.9 s, v5 3.2 s / 2.2 s, v6 3.1 s / 2.3 s (synthetic / real), against the 30 s budget. Throughput is ≈ 250 output tokens/s.
  - Tests turn packing off through a mocked `promptFeatures` where two windows are needed, and have explicit packing cases. ARCHITECTURE wording: core request extract#3.
- **X21 Validation and guard additions** (critic round 1 #5, #6 and deterministic residue).
  - `supersedesClaimId` must belong to a confirmed claim of the claim's own person; otherwise it is stripped (`unknown_supersedes`).
  - New-person labels go through the sensitive guard: sensitive digits and address parts are cut out, or the person is dropped with all its items.
  - parent/child with an extended-kin label (爷, 奶, 外婆, 叔, 姑, 亲家, …) → `relative`.
  - `other` relations need a relation word as label: no label or a person's name → dropped (`invalid_item`).
  - A solar date next to a lunar date of the same person and kind on the same messages is the model's conversion and is dropped (`invalid_item`).
  - Within one window, a claim contained in a more specific claim of the same person merges into it.
  - A mobile number is rewritten to "提供过手机号" (more specific than "提供过联系方式", which stays for other phone numbers). The eval judge scored "提供过联系方式" only less_specific against a phone-number fact.
- **X22 extract.v4** (record `20260915-114324`, compare `compare-extract.v3-vs-extract.v4-*`).
  - Changes: packed windows; a recall push (per-person fact checklist: tenure, past jobs as "曾在…工作", education split into school/major/city, stated preferences and health, group roles); unnamed family members as persons; service_provider toward self; @/mention handles.
  - Synthetic: claims P 0.74 / R 0.81, handles P 0.86, relations P 0.68, transactional 0.09.
  - Real: claims P 0.60 / R 0.33, relations P 0.33.
  - Model behaviour:
    - with more context and a checklist it states many more facts, but also plans, dated offers and commute details;
    - unnamed-kin persons ("<name>+儿子/老公") broke person matching and produced parent/spouse relations to phantom persons;
    - a passing "你儿子" again created a person;
    - residence was inferred from a delivery address;
    - grandparents were typed parent despite the rule.
- **X23 extract.v5** (record `20260915-115019`, compare `compare-extract.v4-vs-extract.v5-*`).
  - Changes: no unnamed children or spouse persons (a spouse's fact goes on the speaker as a family claim); no third-party news persons, business cards or ads; service_provider needs an actual service to that person, applies in group chats too, and a service toward "我" always yields the relation plus a "做什么" claim; no plans; no residence from addresses, locations or links; a separate "在上学，是学生" status claim; "提供过手机号".
  - Synthetic: claims P 0.74 / R 0.77, handles P 0.85, relations P 0.93 (passes), dates P 0.86 / R 1.00, transactional 0.08.
  - Real: every gate passes (claims P 1.00 / R 1.00, handles P 1.00, relations P 1.00).
  - Remaining synthetic FPs:
    - the forced "在上学，是学生" status on children whose grade or school was already known (4, judged over-inference);
    - a child's fact phrased "儿子…" on the parent;
    - "这两天牙疼";
    - "将搬去…" / "在某地工作" / "住在某地" around a relocation (transactional);
    - over-literal restatements.
  - Handle FPs are kinship address terms assigned to a family member other than the one gold uses.
- **X24 extract.v6** (record `20260915-120035`, compare `compare-extract.v5-vs-extract.v6-*`).
  - Changes: v5 trimmed by 26 %; the student status only when nothing more specific is known; children's facts on the named child; a decided plan recorded as "计划…" unless it is fulfilled later in the fragment; an accepted offer as the new role; `mentioned` also covers short names used in the text.
  - Synthetic: claims P 0.81 / R 0.72, handles P 0.79, relations P 0.80, transactional 0.02 (passes).
  - Real: claims R 0.67 (one of 3 gold claims missed; fails).
  - Model behaviour:
    - "给孩子上课 to 是孩子" made the model link a teacher to every child in a class group as service_provider (3 relation FPs);
    - "mentioned also covers short names" did not add true positives;
    - dated offers returned with a converted start date;
    - the kin-term handle errors stayed.
- **X25 Current version = extract.v5; live calls stopped (budget).**
  - Gate count: v3 fails 4, v4 fails 8, v5 fails 3 (synthetic claims P 0.74, handles P 0.85, transactional 0.08; real passes), v6 fails 4.
  - v5 therefore stays `PROMPT_VERSION`; v4 and v6 remain in the registry with cassettes.
  - Loop usage after the v6 run: 2 364 353 / 3 000 000 tokens. Another record eval (≈ 0.16–0.19 M) would cut into the ≥ 600 k reserved for critics, so round 2 makes no further live calls.
  - The critic can replay v5 and v6 from `fixtures/cassettes/{synthetic,real}/extract.v5|v6` and `dedup.v2`; both replays reproduce their record runs with 0 misses and 0 judge fallback.
  - Blocker for the synthetic gates: prompt iteration needs live budget in the next loop.
  - Candidate next steps (generic):
    - drop the forced student status;
    - keep v6's plan/fulfilment rule and the offer-as-role rule;
    - revert v6's child-as-served-person rule;
    - a deterministic check that an address term's evidence is addressed to that person (reply-to or @) before keeping kin terms in group chats.

Round 3 (2026-09-15).

- **X26 Deterministic precision rules after validation** (critic round 2 #1, #2, #3, #5). No prompt change: extract requests stay byte-identical to the extract.v5 cassettes, so the rules are measured on exactly the recorded model output.
  - Error analysis of the v5 false positives (synthetic report `20260915-120617`) showed repeated, model-level patterns that prompt wording in v3–v6 did not remove. The rules are generic and apply to every version:
    - **Group-chat parent/child vocatives are not handles** (`ambiguous_handle`): `address_term` values 爸/爸爸/老爸/爹/妈/妈妈/老妈/娘/儿子/女儿/闺女/儿 in a group chat are dropped; private chats keep them. Why: each speaker means a different person, while a handle is unique per (owner, kind, value, chat) (SPEC data model), so the first speaker's usage became everyone's alias (both v5 handle FPs; also the reason a correct "妈" for another member could never be stored). The kinship stays in relations. Cost: correct vocatives in family groups are lost too (synthetic handles recall 0.50 → 0.42; precision 0.85 → 1.00).
    - **Confidence floor 0.85** (`low_confidence`). Calibration on v5 synthetic outputs: the model's 0.6 claims (2) and 0.8 claims (unique statements: 5 FP, 1 TP) were almost all false positives (residence inferred from a shop location or a link, "换了新学校" from "新学校", a one-off wish read as a preference); all real-data claims were ≥ 0.85. This is a threshold chosen on eval data; a future prompt version should align its confidence scale with it.
    - **Momentary and not-yet-happened statements** (`momentary`): time words (这两天/今天/明天/刚才/这周…) anywhere in a statement, or a leading 将/将要/即将/准备/打算.
    - **Generic student status** ("在上学，是学生") dropped (`redundant`) when the same person has a grade or class in the window, among confirmed claims, or among this import's proposed claims (checked in the pipeline against the dedup candidates), or when none of its evidence messages mentions schooling at all. Major, school or "在某大学读书" alone do not make it redundant.
    - **`other` relations** whose label contains the name of either person are dropped (was: equal to a name), together with a claim of `from` with the same text on the same messages (the same inference emitted twice).
    - **A child's fact phrased on the parent** ("儿子…/女儿…/孩子…") moves to the child, statement without the prefix, when the window has exactly one parent/child relation from that person; otherwise it stays.
  - New `DropReason` values (additive): `low_confidence | momentary | ambiguous_handle | redundant`. ARCHITECTURE wording: core request extract#4.
  - Result (extract.v5 cassettes, report `20260915-123053`, replay `20260915-123123` identical, compare `compare-extract.v5-vs-extract.v5-20260915-123204.json`):
    - synthetic: claims P 0.74 → 0.90 (strict 0.81), R 0.77 → 0.74; handles P 0.85 → 1.00, R 0.50 → 0.42; relations P 0.93 → 0.96; transactional 0.082 → 0.02; dates, events, sensitive 0, invalid evidence 0 unchanged. All synthetic gates pass.
    - real: unchanged, every gate passes.
  - Remaining synthetic claim FPs (5): an internship detail split into a second claim, "喜欢恐龙衣服"-type literal restatement, a reversed "helped parents find a flat", an offer recorded although the chat later shows the job started, "在深圳工作"-type relocation logistics. These need prompt work (offer → current role once started; merge one experience into one claim), which needs a live record run the loop budget does not allow this round.
- **X27 Top-up eval script** `server/extract/scripts/eval-fill.ts` (run with `node --import tsx`).
  - Replays the committed extract cassettes of the current prompt version and makes recorded live calls only for other cassette misses (dedup inputs change when validation drops claims) and judge cache misses; live extract calls are refused; `--max-live-tokens` caps live usage.
  - After it, a plain `pnpm eval` replay hits every cassette and judge entry (verified: 0 cassette misses, 0 judge fallbacks, 0 tokens).
  - Round 3 live usage: 18 912 + 7 590 tokens (two top-up runs, dedup and judge only). Loop total 2 390 855 / 3 000 000, keeping the ≥ 600 k critic reserve.

## import

- **I1 Alignment (SPEC §8.4).** `alignMessages` is an exact LCS over fingerprint sequences: drop elements whose fingerprint does not occur in the other sequence (a disjoint re-import stays O(n)), trim the common prefix/suffix, then Myers O((N+M)·D) on the rest. Past D = 1000 it falls back to a greedy in-order match, which is not optimal; this is recorded because it only matters for pathological reorderings. Same-minute duplicates (two "嗯") match pairwise and are never collapsed. Unmatched messages go after the last stored message sent at or before them, between the matched anchors. This uses an optional `sentAt` on `existing`, additive to the §1.4 signature (core request import#3). New seqs are spaced evenly in their gap. When a gap is too small, the chat is **renumbered** (round 2, replaces the ×1024 rescale): stored message k → (k+1)·unit, unit = smallest power of two ≥ (largest bounded gap + 1)·1024; stored seqs above 2^44 (legacy rescales) also trigger it. Old values are never multiplied, so repeated prepends stay bounded (max ≈ stored count × unit); every seq is asserted ≤ 2^52. See I15 for the D1 side. The renumber runs **before** the inserts, so the new seqs are computed against the renumbered values.
- **I2 Mapping claim.** `POST /mapping` claims the import with a conditional `mapping → parsed` update, so a double submit cannot insert twice. On any failure it deletes the rows it inserted (messages with `first_import_id`, links, attachments, jobs, handles/persons/chat it created) and returns the status to `mapping`. The staged payload is kept, so step 2 can be retried. `check` and `POST /api/imports` treat `parsed` like `mapping`: stale, never a duplicate.
- **I3 Seam with extract.** `createJobsForImport` reads `imports.chat_id` (409 when null), so mapping links the chat just before creating jobs. `extracting` is still set in the final batch. The integration test uses extract's real function, not a mock; the mock had hidden exactly this.
- **I4 Delete-import extension (§11).** Mapping creates display handles with `source_kind='manual'` and `import_id` = the import. They carry no evidence, and `ai` would make §11 step 3 delete them while other imports still use them. Deleting an import also deletes those handles when no remaining message uses them and they have no evidence outside M. Without this, a person created by the import could never be removed while the chat survives. Round 2 closes the A→B gap: before the import row is deleted (FK `set null`), manual display handles and non-self persons with `import_id` = this import are handed to the earliest other import that still uses them (handles: an import linking a message they sent; persons: via their handles' messages, then handles/claims/dates of another import), so deleting that import later removes them as "created by this import". Chosen over a broad "any empty person" sweep so a person the user made by hand is never deleted by an import delete.
- **I5 Sender → person.** Handle kind follows the chosen chat's kind. The value is the raw display name, scoped to the chat, `confirmed`. An existing handle that points at a different person is re-pointed to the user's pick. The first "我" pick creates the self person, labelled with that display name, when none exists. A pick of a merged person resolves to its `merged_into_id`.
- **I6 Chat recommendation.** My own display names (settings `selfDisplayNames`, or any handle of the self person) never count, because I am in every chat. score = matched non-self senders / non-self senders. Step 2 preselects an existing chat only at score 1 or when `open({ chatId })` names one; otherwise it preselects "新建聊天". The kind is preselected as 私聊 for 2 senders and 群聊 for more, with the private title defaulting to the other sender's name. matchReason: `『名字』在这个聊天里出现过` / `『名字』等 N 人…` / `所有发送者都在这个聊天里出现过`.
- **I7 Sender suggestions.** Candidates rank in this order:
  - a display handle in a chat: 在『聊天』中也叫这个名字
  - another handle kind: 也被称为这个名字
  - a label equal to the name: 人物名就是这个名字
  `suggested` is `{self}` when the name is in `selfDisplayNames` or the top candidate is the self person; otherwise it is the top candidate. For a private chat, a sender with no suggestion defaults to 新建人物. In a group chat such a sender starts unselected, so 开始 stays disabled until every sender is chosen (SPEC §9.7).
- **I8 Step 1.** The attachment list only offers media that some message refers to, because attachment rows need a message. Images start checked; videos and files start unchecked. Images over 20 MB and videos over 100 MB are disabled. A failed `check` never blocks the preview, since `POST /api/imports` enforces uniqueness again. Parse timing marks are `xiaoli:parse:start` (file chosen) and `xiaoli:parse:end` (preview or duplicate notice painted). The latest `import/parse-perf` median is 143 ms for perf-5000.
- **I9 Upload queue.** The runner reads the ZIP `File` once per run (`readMediaFiles`) and PUTs one attachment at a time as `application/octet-stream`; the server falls back to the row's mime. Network errors and 5xx get 2 retries (600 ms, 1800 ms); 400, 404 and 413 are not retried. If the same name is on several rows of one import, it uploads once and all those rows get the R2 key. The PUT answers 404 for names that are not selected in this import, and 409 while the import is `mapping`/`parsed`.
- **I10 Upload status line.** When GET detail fails and this tab has no runner, the line reads "上传状态没有加载出来 · 重试" (inline error, ARCHITECTURE §3). With no known names it says 图片. Until import-result (wave 3) mounts `<AttachmentUploadStatus>` on `/imports/:id`, F5 is verified on `/dev/import/upload-status`, which also lists this tab's running uploads so a client-side back navigation shows live progress.
- **I11 Drop handling.** While the overlay holds a created import (step 2 or submitting), drops and `open()` are ignored and no veil is shown, so an import is never orphaned without the abandon DELETE. Closing at step 2 sends `DELETE` with `keepalive` (A7 #2).
- **I12 D1 batches.** drizzle's `db.batch` crashes on `db.run(sql\`…\`)` items ("reading 'bind'") while the same statement runs fine on its own. Every batched statement in import therefore uses the query builder, with `sql` fragments only inside `where`/`set`. Found by the delete-semantics integration test.
- **I13 Client HTTP.** The overlay uses a small typed `fetch` helper instead of `api` + `unwrap`, because of the `unwrap` typing problem (core request import#1).
- **I14 Real samples (numbers only).** `npx tsx server/import/scripts/check-real-import.ts` parses each real ZIP in Node and imports it through the running app into a throwaway account, without uploads. For all 3 files, parsed, import, new and chat message counts are equal (33 / 37 / 66). A duplicate POST answers 409, and deleting removes all messages and the chat.
- **I15 Renumber in D1 (round 2).** One atomic batch: (1) the extraction job windows of every import of this chat are moved to the new numbering (bound = rank of the stored message with that seq via one materialized `row_number()` ranking; a bound that is not a stored seq falls back to counting: first message ≥ start, last ≤ end), so pending/failed jobs keep exactly the same messages; (2) `UPDATE messages … FROM (select id, row_number() over (order by seq))` to negative temporaries; (3) negatives → rank × unit. 50k messages + 2k jobs: ~130 ms + ~60 ms in the local D1. `server/import/imports.test.ts` checks each job's `loadWindow` message ids are unchanged after a 1,500-message prepend.
- **I16 Mapping rollback is complete (round 2).** `Created` also records re-pointed handles (previous person/status), attachment ids re-selected on reused messages, and — after the renumber batch commits — the old seqs and the old job windows. `rollback` restores them; old seqs are compressed into arithmetic runs over the rank (old = a + b·rank, one two-phase update per run), so a typical chat needs a handful of statements. Test: `server/import/mapping-rollback.test.ts` (createJobsForImport made to throw once).
- **I17 Overlay details (round 2).** Sender picker column is 300px so the 300px PersonPicker popover (align start, owned by search) ends at the trigger's right edge inside the overlay; no core/search request needed. Step-2 footer names the real blocker (给新聊天起个名字 / senders / both). Attachment groups show the group total (`共 84 B`), or `selected / total` when partly selected. The dialog focuses itself on open (Radix `onOpenAutoFocus` → content) so × never shows a focus ring after a drop.

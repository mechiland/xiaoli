# Core requests — parser

## #1 ARCHITECTURE §1.2 fingerprint text; backfill; gold anchors after PARSER_VERSION wechat-export@2
- status: done
- requested-by: parser, overall-critic fix round 1, 2026-09-16
- kind: contract
- paths: ARCHITECTURE.md §1.2 (text); eval/gold/**/anchors (owned by eval-synthetic / annotator); deploy runbook (remote D1)
- change:
  1. §1.2 `fingerprint` comment: "FNV-1a 64 hex of `${senderName}${sentAt}${kind}${fingerprintBody(kind, body)}`, sync. For kind image/video, `fingerprintBody` replaces each generated media file name `微信(图片|视频)_\d{8,14}(_\d+)?.<ext>` with `微信图片_*` / `微信视频_*`. For every other kind it is `body`." Add `fingerprintBody(kind, body): string` to the list of public exports. `PARSER_VERSION` is now `wechat-export@2`. `messagesDigest` is unchanged.
  2. Stored rows: after deploying this parser, run `npx tsx lib/wechat-export/scripts/backfill-fingerprints.ts --remote [--env <name>]` once against every D1 that already holds messages. It is idempotent and prints counts only. The local dev D1 is already done (261 rows).
  3. Gold anchors: `anchors[].fingerprint` of 3 gold files (2 synthetic, 1 real) sit on an image/video message and no longer match the parser. Messages, idx and messagesSha256 are unchanged. Either refresh those anchor fingerprints mechanically with the current parser (not an annotation change), or have `checkGold` compare anchors only when `messagesSha256` differs. Recommended owner: eval-synthetic; the integrator decides whether the gold-change rule applies.
  4. For verify-seed: `scripts/seed/dataset.ts` `fingerprint` duplicates the v1 formula. It should import `fingerprint` from `@/lib/wechat-export`, or mirror the P14 normalisation.
  5. For import (optional hardening): `server/import/mapping.ts` trusts the client-computed `payload.messages[].fingerprint` and the stored column. Two changes would keep alignment right even with a stale staged payload from an older bundle, or an unbackfilled row. (a) Recompute `fingerprint()` for incoming messages server-side. (b) Recompute it for stored image/video rows when aligning.
- why: overall critic r1 [major]. A later export renames every image/video by export time, so a re-import duplicated them (SPEC §8.4, M1). DECISIONS parser P14.
- workaround: parser change, tests and local backfill are done inside `lib/wechat-export/**`. Nothing to remove.
- blocking: no for the app. Yes for `pnpm eval:validate-gold` on the 3 affected gold files until item 3 is done.
- Resolution (integrator, wave 4, 2026-09-16): (1) Done: ARCHITECTURE §1.2 fingerprint comment uses `fingerprintBody(kind, body)`, `fingerprintBody` added to the public exports, `PARSER_VERSION` note says `wechat-export@2`, plus a paragraph on the backfill and on importing `fingerprint` instead of copying it. (2) Routed to deploy's STATUS openIssues (run the backfill with `--remote` once per D1 that already holds messages; add it to docs/DEPLOY.md). (3) Option (b), no gold change: anchors only locate drift. The gold-change rule does not apply because no gold bytes change. §7.2 and the §7.6 validate-gold text now say anchor fingerprints are compared only when messageCount/messagesSha256 differ. Routed to eval-synthetic: `checkGold` in `eval/src/annotate.ts` (`run.ts` already behaves this way). (4) Routed to verify-seed: `scripts/seed/dataset.ts` imports `fingerprint` from `@/lib/wechat-export`. (5) Routed to import as optional hardening (recompute incoming fingerprints server-side; recompute stored image/video fingerprints when aligning). DECISIONS integrator I17.
- Follow-up (integrator, wave 4 end, 2026-09-16): The routed items owners had not landed were closed as minimal seam fixes. (3) `checkGold` in `eval/src/annotate.ts` compares anchors as errors only on drift and otherwise warns; a unit test was added in `eval/tests/gold.test.ts`. `pnpm eval:validate-gold`: 7/7 files, 0 errors. (4) `scripts/seed/dataset.ts` re-exports `fingerprint` from `@/lib/wechat-export` instead of its copy. (2) docs/DEPLOY.md §5 has the one-time `--remote` backfill step. (5) stays open as optional hardening for import. DECISIONS integrator I20.

// BOOTSTRAP PLACEHOLDER created by core (ARCHITECTURE §1.1). Owner: settings — overwrite freely.
import { Meta, PageShell } from '@/components/loam'

export default function SettingsPage() {
  return (
    <PageShell title="设置" width="reading">
      <Meta>这一页还在建设中</Meta>
    </PageShell>
  )
}
